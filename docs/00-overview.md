# 00. 全体像

## 目的

資金50万円で日本株を自動売買し、収益を目指す。

## 進め方

```
Stage A（口座なし）  設計 → 構築 → ヒストリカル検証
                        ↓ 見込みがあれば
Stage B（口座あり）  ペーパートレード3ヶ月 → 実弾（少額から段階的に）
```

**Stage A は証券口座もAPIも不要で着手できる**（データ源は J-Quants 無料と yfinance）。
戦略に見込みがなければ、口座を開く必要すらない。

Stage B で架空資金のペーパートレードを3ヶ月行い、
事前定義した合格基準（07-go-live-criteria.md）で実弾移行を判定する。
**Stage B でも運用資金は入れない。**

詳細は 09-data-sources.md。

---

## 中核となる設計方針 — レバレッジ1倍固定の信用取引

**建玉総額（買建 + 売建の合計） ≤ 現金残高。**

信用取引を使いながら、使用金額を現物と同じ水準に抑える。
リスクを現物並みに保ったまま、信用取引のメリットだけを取り込む設計。

| 得るもの | 内容 |
|---|---|
| 同一銘柄の当日複数回転 | 現物の差金決済規制（1銘柄1日1回転が上限）を回避 |
| 空売り | 下落局面でも収益機会。相場環境への依存を減らせる |
| 手数料0円・金利0%・貸株料0% | デイトレ信用の条件。実質コストはスリッページのみ |
| オーバーナイトリスクなし | 当日決済。場が閉じている間の暴落に晒されない |

詳細と、この方針が生む新たなリスクは 02-margin-rules.md を参照。

---

## 対象市場 — 日本株に限定

本プロジェクトは日本株（東証）のみを対象とする。

### 米国株を検討した結果（将来の再検討用に記録）

2026年6月4日に FINRA 規則4210 が改正され、デイトレードの $25,000 最低証拠金規制
（PDTルール）は撤廃された。小資金でも米国株のデイトレードが制度上は可能になっている。

しかし **50万円規模では日本株が明確に有利**：

| | 日本株（eスマート証券・デイトレ信用） | 米国株（IBKR Pro） |
|---|---|---|
| 取引手数料 | **0円** | Fixed $0.005/株・**最低$1/注文**<br>Tiered $0.0035/株・最低$0.35 + 諸費用 |
| 10万円ポジションの往復コスト | **0%** | **0.11〜0.30%** |
| 為替リスク | なし | USD/JPY が年10%規模で変動 |
| 実行環境 | Windows常時起動が必須 | IB Gateway は Linux 可 |
| 市場時間 | 9:00-15:30 JST | 22:30-05:00 JST（深夜・介入不可） |

**IBKR の「最低$1/注文」が決定的。** 1ポジション $660（約10万円）の場合、
$50 の株なら13株で手数料は実額 $0.065 ではなく**最低額の $1 に張り付く**。
往復 $2 = **0.30%**。最低額が効かなくなるには200株以上必要だが、
それは $10,000/ポジション = 口座資金の3倍。
**50万円では最低手数料が構造的に必ず効き、デイトレの利幅0.3〜0.5%の大半を食う。**

さらに、3ヶ月のペーパートレード評価において**為替変動が成績のシグナルをノイズで覆う**。
USD/JPY が3ヶ月で5%動けば戦略の実力が見えなくなる。

### 再検討の条件

- 資金が **$25,000（約380万円）規模に育つ** → IBKR の最低手数料が効かなくなる
- 戦略を**低頻度のスイングに振る** → 往復0.1〜0.3%が相対的に無視できる

`broker/base.py` の抽象インターフェースにより、将来の移行は Broker 実装の追加だけで済む。

> 「米国株の方が低リスク高パフォーマンス」ではない。米国市場は長期的に
> 右肩上がりでロングバイアス戦略に追い風だったが、それは「機会が多い」ことを意味しても
> 「リスクが低い」ことは意味しない。**ボラティリティが高い＝リスクが高い**。

---

## 手法の位置づけ

| 手法 | 内容 | 位置づけ |
|---|---|---|
| **竹** | デイトレード ロング/ショート（当日決済） | ★**メイン。最初から作る** |
| 松 | スキャルピング | 将来的に検討（竹が実弾で安定稼働した後） |
| 梅 | 日足スイング ロング/ショート | 資金300万円以上で追加 |

詳細は 04-strategies.md。

---

## 目標リターンと現実的な評価

**目標は年利25%、ストレッチは年利35%**（2026-08-27 に変更・意思決定ログ73）。

| | 年利 | 月利換算 | 1日平均 | 50万円での日次 |
|---|---:|---:|---:|---:|
| 目標 | **25%** | 1.88% | 0.093% | 約465円 |
| ストレッチ | 35% | 2.53% | 0.125% | 約626円 |

**当初は月利5%（年利79.6%）を目標に置いていたが、年利で見ると過大だった。**
月利3%でも年利42.6%になる。月利ベースで考えると小さく見える数字が、
複利で年に効くと非現実的な水準になっていた。

信用・レバ1倍・手数料0・1日複数回転・空売り可という条件では、
**年利25〜35%は上級者水準だが非現実的ではない**。
3ヶ月のペーパートレードで実測する。

> **評価軸はリターンそのものではなくシャープレシオ**（リターン ÷ 変動）に置く。
> リターンだけを追うと、たまたま勝った高リスク手法を選んでしまう。

---

## 工程

**2段階構成。Stage A は口座もAPIも不要で、今すぐ着手できる**（docs/09-data-sources.md）。

### Stage A — 口座なし・APIなし

データ源は J-Quants 無料（メール登録のみ・証券口座不要）と yfinance。

| Phase | 内容 | 完了条件 |
|---|---|---|
| 0 | 設計ドキュメント | ✅ docs/ が揃い、ブランチに push 済み |
| 1 | データ基盤（J-Quants 日足・銘柄一覧 + yfinance 分足） | 日足と5分足がローカルに蓄積。**実機で yfinance の取得可能期間を実測** |
| 2 | ユニバース構築（Layer 1/2） + バックテストエンジン | 日次で銘柄リストが出力。バイアス検査を通過 |
| 3 | 竹の実装 + ヒストリカル検証 | ウォークフォワード検証を通過、成績レポート |

**ゲート: Stage A の成績で、口座を開く価値があるかを判定する。**
見込みがなければここで戦略を練り直す。**口座開設・API申込・Windows常設のコストを払わずに済む。**

### Stage B — 口座あり・APIあり（**運用資金は入れない**）

| Phase | 内容 | 完了条件 |
|---|---|---|
| 4 | kabuステーションAPIラッパー + ペーパー基盤 + **安全装置16項目** | 全安全装置が動作、reconcile と大引け全決済が通る |
| 5 | **3ヶ月ペーパートレード**（竹） | 実測成績が揃う。**システム障害ゼロ**を確認 |
| 6 | 評価 → 改善 or 実弾移行（少額から段階的に） | 合格基準（07）で判定 |

> **口座開設と入金は別。** 口座開設自体は無料・入金不要で、kabuステーションAPI も無料。
> Stage B は「口座は開くが運用資金50万円は入れない」状態。
> ただし Professional プランの条件に信用口座開設と取引実績があるため、
> 最低1回の実取引（数万円程度）が必要な可能性がある（要確認）。

### 将来

| Phase | 内容 | 条件 |
|---|---|---|
| 7 | 松（スキャルピング）の着手判断 | 竹が実弾で安定稼働した後 |
| 8 | 梅（日足スイング）を追加 | 資金300万円以上 |

### Stage A が解いたボトルネック

当初計画では「分足はどこからも取れないので、kabuステーションAPI で
**3ヶ月かけて自前蓄積**してからでないと竹を検証できない」という制約があった。

**yfinance は5分足を過去60日分まとめて取得できるため、待たずに検証を始められる。**
オープニングレンジ・ブレイクアウト（30分レンジ）の検証には5分足で十分。
1分足は直近7日しか遡れないが、日次でローリング回収すれば蓄積できる。

### Stage A の限界（口座なしでは検証できないこと）

| 未検証項目 | 対処 |
|---|---|
| 板情報がない → 約定モデルが粗い | スリッページを**保守的に厚く**見積もる |
| 寄り前気配がない → 選定スコアの2指標が使えない | 前日引け時点の情報だけで選定（docs/09 §5） |
| 売建可能銘柄リストが取れない | 流動性上位で代理。Stage B で実データに差し替え |
| **発注系が未検証**（大引けクローズ・冪等性・reconcile・レバレッジ強制） | **Stage B が必要。** 省くと「システム障害ゼロ」が判定できない |

### 並行して必要な人手のアクション

**Stage A では不要。** 以下は Stage B に入る時点で行う：

- 三菱UFJ eスマート証券の口座開設 + 信用取引口座の開設
- kabuステーションAPI の利用申込（ログイン後画面から設定、書面・捺印不要）
- Professionalプラン条件の充足（信用口座開設 + 取引実績）
- Windows PC でのスリープ無効化・kabuステーション自動起動設定

**Stage A で必要なのは J-Quants の無料登録（メールアドレスのみ）だけ。**

---

## 意思決定ログ

決定を後から辿れるように、根拠つきで記録する。

| # | 決定 | 根拠 |
|---|---|---|
| 1 | 証券会社は三菱UFJ eスマート証券 | 個人向けで発注APIが使える実質的な選択肢。デイトレ信用が手数料0・金利0 |
| 2 | 信用取引をレバレッジ1倍で使う | 差金決済規制の回避・空売り・手数料0を、リスクを現物並みに保ったまま得る |
| 3 | 日本株に限定 | 50万円規模では IBKR の最低$1/注文が構造的に効き、日本株の手数料0に勝てない |
| 4 | 主軸は竹（デイトレード） | 資金効率が最大。コスト0・オーバーナイトリスクなし・空売りで環境依存が小さい |
| 5 | 梅は資金300万円以上で追加 | 梅は分散が効いてこそ機能する。50万円では株価レンジ制約と重なり前提が崩れる |
| 6 | 松は竹の実弾稼働後に判断 | レイテンシ勝負で機関投資家に構造的に不利。竹の実績を見てから投資判断する |
| 7 | 完全自動 + キルスイッチ | デイトレは場中の人的介入と相性が悪い。安全装置を厚くして無人運転する |
| 8 | 株価レンジは2段階（通常枠/プレミアム枠） | 2,000円超は集中リスクが増すため、期待リターン優位がある場合のみ採用 |
| 9 | 評価軸はシャープレシオ | リターンだけを追うと、たまたま勝った高リスク手法を選んでしまう |
| 10 | 工程を Stage A/B に分割 | 戦略の見込みが立つまで口座開設・API申込のコストを払わない。見込みがなければ口座は不要 |
| 11 | Stage A のデータ源は J-Quants + yfinance | どちらも証券口座不要。yfinance の5分足60日により、分足の3ヶ月蓄積待ちが不要になった |
| 12 | Stage A の選定は前日引け時点の情報のみ | 寄り前気配が取れないため。Stage B で寄り前気配を追加し、予測力を比較検証する |
| 13 | **竹は板情報をシグナルに使わない** | 板の情報は数秒〜数分で陳腐化し、保有数十分の竹とは時間軸が合わない。板を見る手法ほどレイテンシ勝負になり個人が不利。板は執行と Stage B の品質フィルタでのみ使う（`docs/04` 参照） |
| 14 | 日足は J-Quants を正とし yfinance で直近84日を補完 | Free の12週遅延により、5分足のある期間の日足が取れないため。重複期間で乖離を実測し、Light 課金を数字で判断する |
| 15 | データソースも ABC で抽象化しフォールバック構成に | yfinance は非公式で、仕様変更により数日間データが取れないことがある。単一データソースに依存しない |
| 16 | **配当調整を行わない**（分割のみ調整） | 実測で J-Quants と yfinance の乖離が配当調整の有無だけで説明できた（調整なしで 0.0000%）。基準が違うと日足の継ぎ目に架空の値動きが生まれる。必要なのは実際に約定する価格 |
| 17 | **J-Quants は Free を継続**（Light に課金しない） | 基準を揃えたら乖離が完全にゼロだった。**分割調整は JPX 公式と一致しており品質問題はない**。推測ではなく実測が根拠 |
| 18 | **Layer 1 の通過数が想定外（287 vs 100〜200）でも閾値を変えない** | 外れたのは実測ではなく**未検証だった見込みのほう**。閾値を動かして数字を想定に寄せるのは成績に寄与しない。合否は「目的を満たすか」で判定する（下限100=監視枠50の2倍、上限500=絞り込みが機能する水準） |
| 19 | **Layer 2 のスコアは順位変換してから加重合計する** | ATR%(0.02前後)と出来高比(1.0前後)はスケールが2桁違い、生値を足すと重みに関係なく出来高比が支配する。順位に揃えれば重みが意図どおり効く |
| 20 | **ATR% の足切りはスコア計算の後に行う** | 先に母集団を削ると、残った銘柄の順位が「何を削ったか」で変わる。順位変換は母集団の切り方に依存するため、順序を固定して再現性を担保する |
| 21 | **1銘柄あたり上限25%は維持し、株価上限を3,000円→1,250円に下げる** | 25%は日次ブレーカー(-2%)と1.5×ATR損切りからの逆算値だった。40%だとATR4%で、60%だとATR3%で1敗が当日を終わらせる。緩めることは実質的にブレーカーの無効化。株価3,000円は資金50万円では実現できない（1単元が資金の60%） |
| 22 | **母集団は流動性下限を10億→3億に下げて回復させる** | 上限比率は安全装置だが流動性下限は品質閾値。10〜15万円の注文は3億円銘柄でも売買代金の0.05%で板を動かさない。実測で55→133銘柄。緩めたぶんは約定モデルを厳しくする（5億円未満に+5bps） |
| 23 | **枠の境界1,000円は `max_concurrent=5` から導出する** | 5銘柄同時に持つには1銘柄 ≤ 100%/5 = 20% = 10万円 → 株価1,000円。分布を見て決めると後から「なんとなく」動かす理由が生まれるが、安全装置から導けば動かすには安全装置を変える判断が要る |
| 24 | **ATR% に上限5.33%を設ける** | 下限2%はコスト負けの回避だが、上限は日次ブレーカー(2%)÷(上限比率25%×損切り1.5倍)からの導出。スコアはATR%に最大の重み(0.40)を置くため、上限がないと選定自体が「1敗で当日が終わる銘柄」を上位に押し上げる。実測でATR%最大17.12%、上位10のうち2つが1敗-2%超だった |
| 25 | **1日のトレード数想定を5〜15から3〜5に下げる** | 単元100株が最小単位なので1敗を0.6〜1.9%より小さくできず（実測 中央値-0.99%）、**勝ちを挟まず2連敗すると-2%ブレーカーに達する**（ブレーカーは損益判定であって敗数ではない。利確2.5×ATR/損切1.5×ATRなので1勝が約1.7敗を打ち消す）。ATRベースのサイジング（1敗を0.5%に固定）も試したが、最安の1単元でも予算を超え上位10銘柄すべてが0株になった。**月利目標は動かさない** — 届くかはPhase 3で測る |
| 26 | **連続損失ブレーカー(#5)に損失額の条件を足す（3連敗 かつ 累積-5%）** | 日数だけだと実測で -0.87%/-0.62%/-0.54%（合計-2.0%）の3日で発動し、39営業日のうち7日しか検証できなかった。モンテカルロでは下落日率40%の良い戦略でも60営業日で93%発動する。再開に人の承認が要るので「完全自動＋キルスイッチ」が成立しない。-5%は日次(-2%)の2.5倍・累積DD(-15%)の1/3。日次と累積DDが背後に残るので守りは薄くならない |
| 27 | **トレード数の目標値を持たない** | 実測は1日12.7〜13回で日次ブレーカーは0日。一度「3〜5回」に下げたがあれは全敗を仮定した最悪値だった。縛るのは日次損益(-2%)であって回数ではない。回数を目標にすると勝てる場面を見送るか無理に回すかになる |
| 28 | **#5 を「3日連続」から「直近15営業日の累積 -5%」に変える** | 損失額の条件を足して誤発動は消えたが、今度は見逃しが出た。実データで -0.46%/日 の消耗が続いたとき3日連続では -1.37% にしかならず、累積DD の -15% まで誰も止めなかった。連続性を要求せず「一定期間でどれだけ削られたか」で判定する。三層が -2%/1日 → -5%/15日 → -15%/期間無制限 と段階的になる |
| 29 | **現設定は「運が悪い」ではなく構造的に負けていると判定する（口座はまだ開かない）** | 実測の勝率33.3%（n=507）の95%信頼区間 [29.3%, 37.5%] が、コスト込みの損益分岐 40.5% の**完全に外側**にある。損益分岐は損切1.5×ATR・利確2.5×ATR・往復40bps・ATR%中央値3.33% からの計算値でサンプル期間に依存しない。PF 0.58 も同じことを言っている（総利益を72%増やすか 総損失を42%減らす必要がある）。**ただしパラメータは未検証なので戦略の否定ではない**（Phase 4）。この判定が Gate ①（口座を開く価値があるか）の入力になる。再現は `python scripts/analyze_expectancy.py` |
| 30 | **証券会社を変えてもコストは下がらない** | モデルの40bpsは全額スリッページで、手数料はすでにゼロ（デイトレ信用は手数料0・金利0・貸株料0）。松井証券の一日信用も同条件で、**すでにゼロのものはゼロより安くならない**。スリッページは証券会社ではなく市場の相手方に渡る。むしろ松井は建玉持ち越しのペナルティが1注文3,575円（税込）で三菱UFJ eスマートの2,200円より高い。証券会社選びが効くのは**発注APIの有無**だけ |
| 31 | **約定コストを固定bpsから呼値(tick)ベースに変える** | 往復コストの正体は `スプレッド円 ÷ ATR円` で、**株価が式に出てこない**。600円×ATR3.33% と 2,200円×ATR0.91% はどちらもATR20円で同じコストなのに、固定20bpsだと前者を安く、後者を高く見積もる。**この誤差があるかぎり株価帯を変える提案を評価できない**（旧モデルでは2,200円の銘柄も600円の銘柄も同じコストになる）。ATR%の下限2%も同じ理由で円建て（ATR円 ≥ スプレッド円×5）に作り替えた。`src/autotrader/tick.py` |
| 32 | **同時保有数を絞ってもコストの理論的下限は動かない（が、実在銘柄の分布は変わる）** | `ATR円上限 = 資金 × 日次ブレーカー ÷ (単元 × 損切倍率) = 66.7円` で**建玉比率が約分で消える**。集中すると高い株が買える利得と ATR%上限が下がる損失が正確に打ち消し合う。ただし N=2 なら「2,200円かつATR2.0%」（プライム中大型に普通にある）で天井に近づけ、N=4 なら「1,250円かつATR5.33%」（極端に荒い小型株）が要る。**どちらの母集団が厚いかは計算では決まらないので実測する**（`measure_universe.py` §8）。安全装置#7は測るまで変えない |
| 33 | **約定コストを実測する（推定をやめる）** | ブレーカー有効だと総リターンが閾値に張り付く。実測で固定20bps -5.38% と呼値モデル -5.21% がほぼ同じになり、**結果から `net = gross - cost` を逆算できない**ことが分かった（3本とも8〜9日目に移動窓#5で停止し、発動値は -5.23% / -5.06% / -5.89%）。`ReplayBroker` が約定ごとに`|約定値 - 基準値| × 数量` を積算し、`BacktestResult.total_cost_yen` で出す。CLAUDE.md の「コスト前 +22.5〜+34.7%（推定）」はこれで実測に置き換わる |
| 34 | **スコアのボラティリティ項を ATR% から ATR円 に差し替える** | 実測で、選ばれた監視50銘柄の往復コストが **0.052〜0.121 ATR と2.3倍ばらついていた**（8012: 1,156円 ATR%3.30% → 0.0524 / 547A: 447円 ATR%3.71% → 0.1206。ATR%はほぼ同じ）。コストを決めるのは ATR円 なのにスコアは ATR% で並べており、**選定がコストを見ていなかった**。**重みの数も0.40という値も変えず指標だけ差し替える**ので新しい自由度は増えない（意思決定ログ19の順位正規化と同じ種類の修正）。旧スコアは `--legacy-score` で再現 |
| 35 | **同時保有2銘柄は現時点では採用しない（安全装置#7は据え置き）** | 実測で PF 0.23（vs 0.45）/ 勝率18.5%（vs 29.0%）/ 監視枠は平均39.8・50枠が埋まったのは39日中2日。ATR%上限が 5.33%→2.67% に狭まる一方ユニバースの ATR% 中央値は 3.50% で、**母集団の半分以上が上限超過で落ちる**。ただし n=65 で信頼区間は重なり、セクション8（母集団の実測）も未実行だったため「N を絞る方向」自体の否定ではない。N=3（比率33% / 株価上限1,667円 / ATR%上限4.0%）を含めて再測する |
| 36 | **「負けの正体はコスト」という見立ては実測で覆った** | 恒等式から「コスト前は +22.5〜+34.7%」と推定していたが、約定ごとに数えたら**コストは資金の16.9%、コスト前は -0.40%**（gross PF 約0.99）。推定は建玉を9.5〜12.5万円と置いたが実際は約8.5万円で、さらに約定モデルの欠陥で実効スリッページが設定の半分しか出ていなかった。**素の優位はほぼゼロ。**`net = gross - cost` の gross はコストモデルに依存しないので、**約定モデルを直してもこの結論は変わらない**。コスト工学（呼値・同時保有数・ATR円スコア）はすべて二次的になった |
| 37 | **約定価格をバーのレンジで頭を抑えるのをやめる** | `min(始値 × (1+率), 高値)` としており、**始値=高値の陰線でスリッページがゼロ**になっていた。実測で往復20.2bps（設定40bpsの約半分）で、規約5に反する方向にモデルが甘くなっていた。高値・安値は**約定値であって気配ではない** — 成行が約定する最良気配はそれより上でありうる。指値の約定判定は「実際に取引が成立した価格帯を通過したか」の話なので、実装するときはレンジで判定してよい |
| 38 | **シグナルに優位があるかをランダムエントリーとの比較で検定する** | gross ≈ 0 でも「優位がない」とは即断できない。損切り1.5×ATR・利確2.5×ATR・180分・14:50クローズという**手仕舞いルール自体が損益を生む**ので、比較対象なしには判定できない。`strategy/random_baseline.py` は `TakeIntraday` を**継承**して `generate` だけを差し替える —— 手仕舞いと再エントリー制限が同一コードであることを構造で保証するため。20〜30シードで分布を作り、竹がどこに落ちるかを見る（1点対1点では運と区別できない）。比較は**1トレードあたり**で正規化する |
| 39 | **現パラメータの竹に優位はない（ランダム検定で棄却）** | 竹の gross/件 -2.3円 に対しランダム20シードの中央値は +17.5円で、**竹は20本中7本しか上回らなかった（35パーセンタイル・中央値以下）**。シグナル別では orb 538件が -10円/件、vwap_reversion 17件が +250円/件で、**負けの本体は順張り**。ただし n=17 では偶然と区別できない。39営業日・in-sample なので「竹の否定」ではなく「現パラメータのこの期間での棄却」 |
| 40 | **`trigger_price` が一度も発火していない（記録のみ・今回は直さない）** | `ReplayBroker` はストップ注文を保存するだけで、手仕舞いは全部 `should_close`（バー終値で判定 → 次バー始値で約定）を通っている。**実測で exit の71%（395/555）が "stop"** なので、最も多い手仕舞いのモデルが実運用と違う。直すと全数字が再び動くため、**逆張り検定の途中で基準を変えない**判断で先送り |
| 41 | **実行結果に必ず版（コミット・日時・dirty）を刻む** | 総リターンが -5.10% → -5.21% → -17.26% → -35.94% と何度も動いており、**過去ログの数字がどの版のものか追えなくなった**。実際に古い推定（コスト前 +22.5〜+34.7%）を現状と誤認して議論を進めてしまった。`src/autotrader/provenance.py` が全スクリプトの先頭で出す。**未コミットなら警告を出す** — その出力はどのコミットにも対応せず再現できない |
| 42 | **変種の検定はランダム分布との比較でしか判定しない** | 絶対リターンで良し悪しを決めると、手仕舞いルール自体が生む損益をシグナルの手柄と誤認する。`--experiment` は全変種をパーセンタイルで並べる。**分布の使い回しは妥当性を確認してから**行い、エントリー確率に敏感なら変種ごとに作り直す（警告して不正な比較を続けない）。**5変種なら偶然95%を超えるものが出る確率は約23%**なので、勝っても in-sample の仮説として記録するだけ |
| 43 | **`--experiment` で ORB反転は棄却、VWAP乖離単独1.5%だけが生き残った** | 竹 555件・35パーセンタイルに対し、ORB反転は565件・75パーセンタイル（順張りの逆張りもランダムと大差なく、反転仮説は否定）。VWAP乖離のみ1.5%は221件・**95パーセンタイル**で唯一生存。閾値を0.7%まで緩めると単調に悪化（95%→80%→75%）し、**n=221がたまたま良かった可能性を否定できない** |
| 44 | **ORB が VWAP乖離を締め出していた** | 竹の混合設定では vwap_reversion が17件しか発火していなかったが、`--disable-breakout` で ORB を切ると同じ期間で **221件**に増えた。`max_entries_per_symbol_per_day=1` により ORB が先にその日の枠を使い切っており、**竹の実測はほぼ ORB だけを測っていたに等しい**。この事実だけでも竹の既定設計を見直す根拠になる |
| 45 | **`--stress-test` で多重比較補正と期間分割を検定に組み込む** | `--experiment` の95%は単純な95%であって、5変種を試した多重比較補正後の閾値ではない（Bonferroni: `1 − 0.05/5 = 99%`）。加えて20シードでは95%と80%を区別する解像度がない（20本中19本上回った、が最大値）。`required_percentile()` / `required_seeds_for()` で変種数から必要閾値と必要シード数を導出し、シード不足なら測れない閾値で判定せずエラーにする。前半・後半への期間分割は**バーを切らず監視リストだけを絞る**（ATR の助走を保つため）。**この検定はまだ実データで走らせていない** — VWAP乖離単独1.5%が本当に生き残るかは次の実行結果を見て判断する |
| 46 | **VWAP乖離単独1.5%は事前登録基準どおり棄却（全期間95.5% < 必要閾値99%）。ただし他の棄却変種とは質が違う** | 実データで `--stress-test` を実行（200シード）。全期間221件・95.5%（必要閾値99%に届かない）、前半112件・66.0%、後半112件・87.0%（両方とも50%は超える）。**基準を緩めない**という事前登録の趣旨を守り棄却を確定。ただし竹本体(35%)・ORB反転(75%)のような「明確な負け」ではなく、**両半期で独立に正の方向が出ていた唯一の変種**だった。現行の竹（ORB+VWAP混合）は Phase 3 の設計として終了。次はエントリー根拠を再設計する（`docs/04-strategies.md` 参照） |
| 47 | **「相場の局面に応じて手法を切り替える」提案は妥当だが、「過去10年分で局面判定システムを作る」は時期尚早と判断** | ユーザー提案（レジーム別の手法選択）を検討。発想自体は業界でも使われるまっとうな考え方だが、今すぐ大規模に実行するのは3つの理由で見送り:①J-Quants無料プランは日足でも過去2年まで、5分足はyfinanceで過去58日までで**10年分のデータが物理的に手に入らない**、②局面×手法の組合せは検定すべき数を掛け算的に増やし、**5変種の比較ですら99%補正が要った現状の資金・データ量では検証しきれない**、③**どの局面でも通用する土台がまだ1つもない**段階で使い分けを作るのは早い。縮小版として、外部データを増やさず手元データだけで**日次の値動きの荒さでVWAP乖離の成績を事後的に分けて見る**診断を先に行うことにした （`src/autotrader/regime.py` / `backtest_take.py --by-regime`）。事後診断と実戦用の先読みフィルタは意図的に分離する（`PointInTimeView` が防ぐルックアヘッドを別の場所で作り込まないため） |
| 48 | **`--by-regime` の初回実装は分類基準がエントリー条件とほぼ同義になっており、実データで実行すると221件中220件が wild・calm 1件という無意味な結果になった。日ごとの市場全体の荒さに作り替えて訂正** | 初回実装は「その日の銘柄群（約130銘柄）を横断した中央値」を境界に**銘柄ごと**に calm/wild を割り当てていた。VWAP乖離のエントリー条件（その日その銘柄がVWAPから1.5%以上乖離＝仲間より大きく動いた）と、旧「wild」の定義（その日の値幅が仲間内の中央値を上回る）が**ほぼ同じものを測っていた**ため、シグナルが発火する取引のほぼ全部が機械的に wild に分類された。「相場の局面（数か月単位）によって成績が変わるか」という元の問いに何も答えられていなかった。**分類の単位を「銘柄ごと」から「日ごと」に作り替えた**——各日の値はその日の全銘柄の実現レンジの中央値（＝市場全体の荒さ）とし、日をまたいだ中央値で calm/wild を二分する。個別銘柄のエントリー条件とは独立した軸になる。ユーザーには訂正版で再実行を依頼 |
| 49 | **訂正版 `--by-regime` で calm/wild の順位差が判定可能になった（calm 73.5%・wild 99.0%）。先読み版フィルタの設計に進む前に、市場の荒さの「持続性」を診断する `_regime_persistence` を追加** | 訂正版を実データで実行（calm 72件・73.5パーセンタイル、wild 149件・99.0パーセンタイル）。前回の欠陥（銘柄単位分類がエントリー条件と同義）は解消され、calm日にも十分な件数のトレードが発生した。**順位ははっきり違う**ので、計画どおり次は「前日までの情報だけで今日を予測する先読み版フィルタ」の設計段階。ただしこれは「市場の荒さは日をまたいで持続する」という前提の上に成り立ち、この前提自体は未検証だった。フィルタを設計する前に、`regime.classify_days` が使う日次集計を `market_range_by_day` として切り出し、`_regime_persistence`（前日wild→当日wildの条件付き確率 vs 前日calm→当日wildの条件付き確率）で持続性を安く確認できるようにした。新しいデータもロジックも要らず、既存のラベルを1日ずらして突き合わせるだけ。ここで持続性が確認できて初めて、実際のフィルタ設計に進む |
| 50 | **持続性診断の結果は統計的に有意でなく（60.0% vs 44.4%、n=20/18、z≈0.96・p≈0.34）、市場全体レジームの先読みフィルタ化はいったん打ち止め** | `_regime_persistence` を実データで実行。前日wild→当日wildが60.0%（n=20）、前日calm→当日wildが44.4%（n=18）。差は15.6ポイントあるが、2標本比率のz検定では z≈0.96・p≈0.34 で、この計画で使ってきたどの閾値（95%・99%）にも遠く届かない。中央値二分の設計上ベースレートは50%付近になるはずで、60.0%と44.4%はその50%を挟んで両側にばらついている形——「wildの翌日はwildになりやすい」という一貫した傾向があるとは言えない。**完全に否定されたわけではない**——n=38ペアという母数の小ささ自体が検出力不足の原因である可能性が高く（`--stress-test` が99%の解像度に最低100シードを要求したのと同じ理由）、80営業日に近づけばより大きな母数で再診断する価値がある。「否定」ではなく「今のデータ量では判定できない」にとどめ、市場全体レジームに基づく先読みフィルタの設計はここで一区切りとし、次の主軸をエントリー根拠の再設計に戻す |
| 51 | **エントリー根拠の再設計・第2ラウンドとして、VWAP乖離を2方向で伸ばす2変種を事前登録（結果はまだ見ていない）** | ORB（順張り）は再挑戦の優先度を下げ、生き残った手がかりであるVWAP乖離（逆張り）を先に使い切る方針。手がかりは2つ：①乖離下限を緩めると単調に悪化した（0.7%→75% / 1.0%→80% / 1.5%→95%）ので、**まだ試していない厳しくする方向**を伸ばす。②市場全体が荒れた日ほど成績が良かった（calm 73.5% vs wild 99.0%、意思決定ログ49）ので、**大きな乖離ほど「本物」らしい**という同じ物語を出来高で確認できないか試す。**変種A**: VWAP乖離のみ2.0%（新規コード不要、`min_deviation_pct`を伸ばすだけ）。**変種B**: VWAP乖離1.5%+出来高確認1.5倍（`TakeIntradayConfig.volume_confirmation_mult`を新設。直近バーの出来高が当日それまでの平均の1.5倍未満なら見送る。新しいデータ源は使わず`Bar.volume`を使うだけ、既定Noneで既存5変種には影響しない）。`EXPERIMENT_VARIANTS`が5→7に増えるため、多重比較補正の必要パーセンタイルは99.0%→99.29%、必要シード数は100→141に上がる（`--stress-test`の既定200シードはまだ十分）。**結果を見る前に**この2変種の定義と仮説を固定する——竹本体・ORB反転のときと同じ、事前登録の規律 |
| 52 | **エントリー根拠の再設計・第2ラウンドの結果: 変種A（VWAP乖離2.0%）・変種B（出来高確認）とも棄却。VWAP乖離の2方向拡張は手がかり切れ** | 実データで7変種を実行。変種A（VWAP乖離のみ2.0%）は87件・65パーセンタイル、変種B（VWAP乖離1.5%+出来高確認1.5倍）は65件・50パーセンタイル（ランダムと見分けがつかない水準）。どちらも既存のVWAP乖離1.5%単独（95%）を上回れず、事前登録どおり棄却。**単に2変種が負けた以上の意味を持つ**——「乖離を厳しくするほど良くなる」という0.7%→1.0%→1.5%で観測された単調な傾向が2.0%で反転し（95%→65%）、出来高確認も同じ「大きく動く＝本物」という物語を支持するはずが単独1.5%より悪化した（95%→50%）。2つの独立した予測がどちらも外れたことで、1.5%の95%という結果自体が「たまたま良かったサンプル」だった可能性が強まった。VWAP乖離という手がかりはこの2方向では使い切ったと判断し、まったく新しいシグナル発想に移る |
| 53 | **ギャップ・フェード診断（`scripts/measure_gap_fade.py`）を、日足のみで安価に予備検証する新しい切り口として導入** | 板情報はStage Aにないため使えない。残る手がかりのうち**ギャップ（前日終値と当日始値の差）はまだ検証していない**うえ、**日足だけで安く予備検証できる**という利点がある——5分足はまだ39〜80営業日しかないが、日足はJ-Quants無料で最大2年（意思決定ログ17）。5分足の検証環境（Layer2選定・約定モデル・リスクチェック）を一切構築せずに、桁違いに大きい母数で先に見られる。**Layer2選定で使う`gap_pct`（寄り前気配ベース、Stage B専用）とは別物**——ここで見るのは当日の実際の始値で、寄り付いた瞬間に確定している情報なので先読みにならない。まだ多重比較補正した合否判定は行わず、閾値バケットごとの`fade_score`平均を目視で確認する段階に留める（`--stress-test`のような検定は戦略化する段階で初めて要る） |
| 54 | **ギャップ・フェード診断の初回実行で閾値を上げるほど単調にフェード側へ振れる結果が出たが、日足キャッシュが想定の約2年ではなく実測で1銘柄あたり約78営業日しかなく`DAILY_LOOKBACK_DAYS`を120→730日に拡張** | `measure_gap_fade.py` を実行（全10,308ペア）。全日ベースラインfade_score平均+0.0040%に対し、`\|ギャップ\|` 下限0.5%→+0.0403%・1.0%→+0.0702%・1.5%→+0.0708%・2.0%→+0.2237%と、これまでのVWAP乖離スイープで見られた反転がなく単調にフェード側へ振れた。**ただし件数10,308÷133銘柄≒78日と、5分足検証窓（39〜80営業日）の2倍程度しかなく、この診断の存在理由だった「J-Quants無料2年分の桁違いの母数」を活かせていなかった。**原因は`scripts/fetch_bars.py`の`DAILY_LOOKBACK_DAYS=120`（Layer2の指標が要る20営業日ぶんを5分足の先頭より前から確保する、という本来の目的にだけ合わせた値）——`measure_gap_fade.py`はこの日足キャッシュを再利用する設計だったため、その浅さをそのまま引き継いでいた。日足はyfinance経由（1d=制限なし、`docs/09-data-sources.md`§2.1）で期間による追加コストがほぼないため、730日（約2年）に拡張した。`BarStore.write`は冪等（タイムスタンプ重複は新しい方で上書き）なので再実行しても壊れない。効果の大きさ自体（+0.04〜+0.22%）は他の診断で見た往復コスト（17〜70bps）と比べてまだ薄いことにも注意 |
| 55 | **日足を2年に拡張してギャップ・フェードを再測定。母数6倍でも単調な傾向は保たれたが、効果の大きさが往復コストと同水準なので `measure_gap_fade.py` にコスト比較と有意性検定を組み込んだ** | `DAILY_LOOKBACK_DAYS=730` で再取得後（63,900ペア、1銘柄あたり約480営業日＝ほぼ2年）、ベースライン+0.0189%に対し0.5%→+0.0498%・1.0%→+0.0842%・1.5%→+0.0818%・2.0%→+0.1572%。**母数が6倍になっても閾値を上げるほどフェード側へ振れる傾向は保たれた**——VWAP乖離が2.0%で反転したのとは対照的で、これまでで最も一貫している。**ただし最大の2.0%バケットでも+15.7bpsで、実測済みの往復コスト（中央値29.7bps・最安17.9bps、意思決定ログ33以降の呼値ベース）と同水準かそれ以下。**「統計的に本物の傾向」と「コスト後に儲かる」は別問題であり、竹の実測でも損益分岐の勝率を割り込んで構造的に負けていた（意思決定ログ36）のと同じ構造。傾向の有無を目視するだけの段階から一歩進め、`bucket_stats` でバケットごとに gross/コスト/net を bps で並べ、標準誤差と t値も出すようにした。コストモデルは `autotrader.tick.spread_yen` をそのまま使い、診断ごとに作り直さない。**net が負なら、傾向が本物でも戦略化する意味はない** |
| 56 | **ギャップ・フェードは全バケットで net 負となり棄却。3実験すべてが「gross は小さく正・コストがそれを上回る」で終わったことから、問題を「シグナル探し」から「コスト構造」へ切り替える** | コスト比較を組み込んで実行した結果、gross は 0.5%→+4.98bps / 1.0%→+8.42bps / 1.5%→+8.18bps / 2.0%→+15.72bps に対し、往復コストは全バケットで約30bps。**net は最良の2.0%バケットでも -14.39bps で、全バケット負。** t値は3.2〜4.9あり**フェード傾向そのものは統計的に本物**だが、コストに構造的に負ける。事前登録した基準（「全バケットで net が負 → 打ち止め」）どおり棄却。**より重要なのは3実験を通じた共通の形**: 竹（gross ≈ -0.3bps）・VWAP乖離単独（gross ≈ +10bps）・ギャップフェード（gross ≈ +16bps）に対し、コストは常に30〜44bps。**見つかる優位が 0〜16bps、コストが 30〜44bps という比率がすべての実験で一定**であり、これは個々のシグナルの良し悪しではなく**コスト構造の問題**を示している。次の一手を「新しいシグナルを探す」から「コストの前提を検証する」に切り替える（意思決定ログ57） |
| 57 | **`DEFAULT_SPREAD_TICKS = 2.0`（プロジェクト最大の当て推量）を、手元の日足だけで検証する Corwin-Schultz 推定を実装** | この1つの数値が**全コスト計算を線形にスケールする**（往復コスト・`min_atr_yen`・Layer 2 のコストスコア・バックテストの約定価格・損益分岐の勝率）。意思決定ログ56で「優位 0〜16bps vs コスト 30〜44bps」という比率が全実験で一定だと分かった以上、**この比率の分母が正しいかを確かめないまま手法探しを続けるのは順序が逆**。`docs/09`・`tick.py` の docstring で以前から「日足OHLCからの推定（Corwin-Schultz 2012）」を候補に挙げていたが、日足を2年に拡張した（意思決定ログ54）ことで**追加データなしに実行可能になった**。`src/autotrader/spread.py` に実装。**実装上の落とし穴を1つ踏んで修正した**——素朴に「2日ペアごとに解いて負を0に切り上げてから平均」とすると壊れるほど上振れする（合成データで真5bps→推定47.6bps、真40bps→推定66.2bpsと、真の値がほとんど反映されない）。**β・γ を先に平均して最後に1回だけ解く**形にしたところほぼ不偏になった（真5bps→3.1bps、真40bps→36.9bps）。なお推定量の性質としてわずかに**下振れ**する（取引がまばらな銘柄ほど日中レンジが狭く観測されるため）ので、**推定値は下限として扱う**——規約「検証できないものは保守的な側に倒す」に従い、「思ったより狭かった」という結果が出ても楽観側へは倒さない |
| 58 | **Corwin-Schultz を原著どおりの夜間ギャップ補正で実装したら実データ133銘柄すべてが負の推定値を返して推定不能になった。前日終値→当日始値の比で補正する形に直して解決** | 初回実行の結果は「推定できた銘柄 0 / 推定不能（負の値）132 / 日数不足 1」。**スプレッドがゼロなのではなく、推定量が機能していなかった。** 原因を合成データで切り分けたところ、**夜間ギャップ**だと判明した——γ は「2日を通した高安」なので夜間に飛んだぶんがそのまま入るが、β は各日の日中レンジの和なので入らない。γ ≫ β となり α が負に振り切れる。原著の補正は「前日と当日の高安が**完全に離れている**ときだけ平行移動」なので、日本株のように**日中レンジと部分的に重なったまま飛ぶ**ケースでは発動せず、γ だけが膨らみ続けていた。合成データでの実測（真20bps・日中ボラ2%）: 夜間ボラ0%→18.6bps / 0.5%→2.9bps / 1.0%→**-34.5bps** / 2.0%→**-120.0bps** と、わずかな夜間ボラで破綻する。**原著が高値・安値しか使わないのは「それ以外が無い前提での一般性」のためであって、あるなら使ってよい。** 我々は始値・終値も持っているので、`前日終値 ÷ 当日始値` の比で当日の高安を割り戻し、夜間の飛びを正確に除く形に変更した。同じ合成データで夜間ボラ0〜3%のすべてで18.6bpsと安定する。テストでも「夜間ボラを0〜3%に振っても推定が動かない」ことを性質として固定した。**「データ不足」と「推定が効いていない」を混同しない**よう、スクリプトの失敗メッセージも分けた（前者は取り直せば直るが、後者は別の問題） |
| 59 | **銘柄ごとに推定して「負を捨てて正だけ平均」していたのは、ペア単位で避けたはずの切り捨てバイアスの再導入だった。全銘柄をまとめて解く `corwin_schultz_pooled` に差し替え** | 夜間ギャップを直した後の実測は「推定できた銘柄65 / 推定不能（負）67 / 正のみの中央値 6.6bps＝0.59tick」。**だが半分が負ということは、正の65銘柄は「ノイズで上振れした側だけが生き残った」標本**であり、その中央値は真の値を表さない。合成データで確認したところ、**真のスプレッドが0bpsでも「正のみの中央値」は +4.8bps を返した**（負の割合87%）。真5bps→+6.1bps、真10bps→+5.6bpsと、真の値をほとんど反映していない。意思決定ログ57でペア単位の切り捨てを避けたのと**まったく同じ罠を、銘柄単位で踏んでいた**。**全銘柄の β・γ をまとめて平均し最後に1回だけ解けば切り捨てが起きない**ので、`corwin_schultz_pooled` を追加してこちらを結果の主役にした。銘柄ごとの推定は「負の割合」（推定がノイズ床にどれだけ近いかの目安）としてのみ表示する。**なお観測された「負51%」を合成データで較正すると真のスプレッドは12〜13bps相当**（負61%＝真10bps、負43%＝真15bps の間）で、呼値換算で約1.1本。想定の2.0本より狭いが、「正のみ」が示した0.59本ほど狭くはない |
| 60 | **Corwin-Schultz はこの銘柄群では推定不能と確定（まとめても -1.3bps）。ただし感度分析により「スプレッド想定を変えても結論は変わらない」ことが分かったので、スプレッドの精密化は打ち止め** | 切り捨てを避けた `corwin_schultz_pooled` でも -1.3bps（-0.13tick）と負。銘柄ごとの負の割合51%から較正した12〜13bpsとも整合しない。**この銘柄群・この推定量では、真のスプレッドは推定量のノイズ床に埋もれている**と結論。板を録る（楽天 RSS 等）以外に Stage A で測る手段はない。**だが「測れない」で止まらず、決定に効くかを確かめた**——gross はコストモデルに依存しないので、スプレッド想定を変えたときの net は確実に計算できる: 竹（gross -0.3bps）は 0.5〜2.0tick のどの想定でも net -11〜-44bps、VWAP乖離1.5%（gross +10bps）も -1〜-34bps で**一度も正にならない**。ギャップフェード2.0%（gross +15.7bps）だけが 1.0tick で +0.7bps、0.5tick で +8.2bps と符号が変わるが、**較正値の約1.1tick では実質ゼロ**であり取引可能な優位ではない。**つまりスプレッドをこれ以上精密に測っても、どの手法も救われない。** `DEFAULT_SPREAD_TICKS = 2.0` は据え置き（規約「検証できないものは保守的な側に倒す」）。感度は `measure_gap_fade.py` の出力に常設した。**結論: 問題はコストの見積り精度ではなく、この銘柄群・この時間軸で得られる優位（0〜16bps）がどんなコスト想定に対しても小さすぎること** |
| 61 | **残る最大のレバーとして TOPIX100 の細かい呼値を実測する（`ScaleCat` を取り込み、`measure_topix100.py` を追加）** | 意思決定ログ60で「スプレッド想定を0.5〜2.0本のどこに置いても、どの手法も net 正にならない」と確定した。**だが呼値そのものは別の話**——東証の呼値は TOPIX100（Core30 + Large70）だけ細かく、`autotrader.tick` に `_TOPIX100_TABLE`（0.1〜0.5円）が実装済みなのに**一度も使われていなかった**（`topix100=True` を渡す箇所がゼロ）。900円の銘柄なら通常の呼値1円で往復22.2bps だが、TOPIX100 は0.1円で**往復2.2bps と10分の1**。**これまでの実験で見つかった優位が10〜16bps なので、この差は net の符号を変えうる。** 制約は株価上限1,250円（資金50万円 × 25% ÷ 100株）と両立するかで、`tick.py` の docstring は「ほとんど入ってこない」と書いていたが**これは推測であって実測ではない**。J-Quants の銘柄一覧には規模区分 `ScaleCat` が含まれている（実測で確認済み・意思決定ログ23の項目一覧に記載あり）のに `Symbol` に取り込んでいなかったので、`scale_category` と `is_topix100` を追加し、`universe.json` にも保存するようにした。**判定基準を先に固定する**: 株価上限・流動性（20日平均売買代金3億円）を通る TOPIX100 が**20銘柄以上なら**既存手法をこの銘柄群で再検定する価値がある、**20銘柄未満なら**分散が効かず安全装置#7（同時保有5銘柄・1銘柄25%上限）と両立しないので打ち止め。なお TOPIX100 は大型株で ATR% が小さい傾向があるため、**コストが下がる代わりに優位も小さくなる可能性**を同時に確かめる必要がある |
| 62 | **`scripts/` が型検査の対象外だったため引数漏れが実行時まで気づけなかった。`py.typed` を追加して `scripts` も `mypy` に含めた（他に4件の潜在バグが即座に見つかった）** | `measure_topix100.py` を実行したところ `list_symbols() missing 1 required positional argument: 'as_of'` で落ちた。**`mypy` の `files` が `["src", "tests"]` で `scripts` を含んでいなかった**のが原因。さらに `autotrader` パッケージに `py.typed` がなかったため、インストール済みパッケージとして import した際に**中身が解析されず**、仮に scripts を対象にしても同じ誤りを見逃していた。`src/autotrader/py.typed` を追加し `[tool.setuptools.package-data]` で同梱、`files` に `scripts` を追加した。**この変更だけで他に4件の潜在バグが見つかった**: `analyze_expectancy.py` で `wilson_interval` に `float` を渡していた（同じ関数の別の呼び出しでは `int()` で包んでいたのに1箇所だけ漏れ）、`verify_data_sources.py` で `date \| None` を `date` 引数に渡していた（`has_range` で守られてはいるが、プロパティ越しなので型検査では追えない形）、`float ** float` が `Any` に落ちる箇所が2件。**手元で回す実測スクリプトだからと検査対象から外していたが、実測スクリプトこそ結果の正しさに直結する。** なお `list_symbols` が `as_of` を必須にしているのは意図的な設計（現在の銘柄一覧を過去に適用するとサバイバーシップバイアスになる・意思決定ログ12）なので、引数を足す形で修正した——契約範囲は400応答から自動学習されるので、範囲外を叩いて学習させてから終端へ寄せる |
| 63 | **TOPIX100 の初回実測「通過4銘柄」は測定のアーティファクトだった（95銘柄が日足未取得）。不足分を取りに行くよう修正** | 実行結果は「TOPIX100 99銘柄 → 通過4銘柄」だったが、内訳は **日足なし95 / 株価上限超え0 / 流動性不足0**。**95銘柄は条件で落ちたのではなく、そもそも日足を持っていなかった**——`universe.json` は Layer 1 を通った133銘柄だけで、`fetch_bars.py` はそれしかダウンロードしないため。**「データがない」を「条件を満たさない」と読むところだった**（意思決定ログ54で日足の浅さを見逃したのと同じ種類の誤り）。むしろ注目すべきは**測れた範囲では株価上限超えも流動性不足も0件**で、`tick.py` の docstring が書いていた「TOPIX100 は株価が高いので上限1,250円とほぼ両立しない」という見込みが**まだ裏付けられていない**こと。実際、通過した4銘柄は540〜1,136円で、往復コストは通常24.9bps → TOPIX100 3.3bps（**10倍の削減**）と、期待どおり1桁下がっている。日足がない銘柄を yfinance から取りに行く処理を追加し（日足は期間の追加コストがほぼないので2年分）、**日足未取得が1/4を超えたら「判定不能」として結論を出さない**ガードも入れた |
| 64 | **TOPIX100 は事前登録どおり棄却（通過7銘柄 < 20銘柄）。ただし阻んだのは呼値ではなく資金——99銘柄中92銘柄が株価上限1,250円を超えた** | 日足を揃えて再測定した結果（日足なし0・有効な測定）、TOPIX100 99銘柄のうち**92銘柄が株価上限超えで脱落**し、流動性不足は0、通過は7銘柄だった。事前登録した基準（20銘柄以上）に届かないので棄却。**だが「呼値の細かさ」という仮説自体は正しかった**——通過7銘柄の往復コストは通常37.0bps → TOPIX100 8.4bps で、期待どおり1桁下がっている（NTT 169円なら118.2bps → 11.8bps）。阻んだのは市場の性質ではなく**`株価上限 = 資金50万円 × 25%（安全装置#7）÷ 100株 = 1,250円`という資金側の制約**（意思決定ログ21）。`tick.py` の docstring が書いていた見込み（「TOPIX100 は株価が高く上限とほぼ両立しない」）は、**これで実測に裏付けられた**。**この制約は安全装置を緩めても解けない**——25%は日次ブレーカー(-2%)と1.5×ATR損切りからの逆算値で、動かすとブレーカーの実質的な無効化になる（意思決定ログ21）。したがって残る変数は資金額だけであり、それは人間が決めること（`CLAUDE.md`「人間が判断すること」）。判断材料として資金→使える銘柄数のスイープを `measure_topix100.py` に追加した |
| 65 | **資金スイープで「TOPIX100 が使えないのは資金の問題」が定量化された。ただし増資を判断する前に「大型株でも優位が残るか」を先に測る（`--topix100`）** | 資金→使える TOPIX100 銘柄数の実測: 50万円→7銘柄(7%) / 80万円→18(18%) / 120万円→35(35%) / 200万円→62(63%) / 300万円→85(86%) / 500万円→88(89%)。事前登録した20銘柄の線は**80〜90万円あたりで越える**。**だが増資は「人間が判断すること」であり、判断材料が足りていない**——これまで測ってきた優位（0〜16bps）は**小型・中型株（ATR% 2〜5.33%）で測った値**であって、TOPIX100 のような大型株で同じ優位が出るかは未検証。大型株はボラティリティが小さいぶん、コストが1桁下がっても**優位も同時に縮む可能性がある**（意思決定ログ61で既に注意として挙げていた）。**幸いこれは資金を1円も動かさずに測れる**——`measure_topix100.py` が TOPIX100 全99銘柄の日足2年分を取得済みなので、その銘柄群でギャップ・フェードを再測定すればよい。`measure_gap_fade.py` に `--topix100` を追加し、`GapFadePair.topix100` でコストも細かい呼値テーブル（0.1〜0.5円）に切り替わるようにした。**net が負なら増資の判断自体が不要になる**（資金を増やしても効かないことが分かるため）。**「資金をいくら入れるか」という答えにくい問いを、「そもそも効くのか」という測れる問いに先に変換する**のがこの順序の狙い |
| 66 | **TOPIX100 でギャップ・フェードが初めて net 正になった（全バケットで正・t値8.5〜10.5）。ただし自分でサバイバーシップバイアスを作り込んでいたので、採用前に2つの検証を追加** | 実測: ベースライン+5.52bps、0.5%→gross+10.41/コスト4.87/**net+5.53**(t=9.9)、1.0%→**net+11.01**(t=10.5)、1.5%→**net+16.54**(t=10.0)、2.0%→gross+24.91/コスト4.75/**net+20.16**(t=8.5)。**プロジェクトで初めての net 正**であり、スプレッド想定を0.5〜2.0本のどこに置いても正のまま。しかも**予想と逆に gross も大きい**（一般ユニバースの2.0%バケットが+15.72bps に対し+24.91bps）——「大型株は ATR% が小さいので優位も縮む」という意思決定ログ61の懸念は外れた。**だが初の good news だからこそ疑う。2つの重大な欠陥を自分で見つけた**: ①**サバイバーシップバイアス**——TOPIX100 の構成銘柄を2026-06-03時点の1枚で取り、それを過去2年に適用していた。今 TOPIX100 にいるのは「2年間 大型で居続けた勝ち組」であり、`docs/03-universe.md` §4.2 が「現在の一覧を過去に適用してはならない」と**明示的に禁じている**ことをそのままやっていた。検証期間の開始時点の一覧も取得して既定で使うようにし（`master_scale_historical.json`）、現在の一覧を使う場合は `--survivorship` を明示させて「採用してはならない」と警告する。②**コスト低下の原因を分解していなかった**——TOPIX100 は呼値が細かいが**株価も高い**。呼値は絶対額なので、株価が高いだけでも bps 換算のコストは下がる（3,000円の通常銘柄なら呼値1円で6.7bps、600円なら33bps）。分けないと「TOPIX100 だから安い」と誤解し、**通常銘柄の高株価帯という選択肢を見落とす**。同じ銘柄・同じ日を通常銘柄の呼値で評価し直す内訳表を追加した。**右端（通常呼値での net）が正なら、本質は呼値ではなく「株価の高い銘柄を扱えるか」＝資金の問題になる** |
| 67 | **事前登録した2つの検証がどちらも返った。サバイバーシップは通過（+20.16→+19.46bps）、呼値の内訳は「細かい呼値が本質」だった（通常呼値では2.0%バケット以外すべて net 負）** | ①**サバイバーシップ検証は通過**。検証期間の開始時点（2024-08-26）の TOPIX100 99銘柄で測り直した結果、ベースライン+5.52→+5.39bps、2.0%バケットの net +20.16→**+19.46bps**（t=8.0）と、ほとんど動かなかった。**この優位は「勝ち組で過去を測った」ことの産物ではない。** 初の net 正という good news を、自分で仕込んだバイアスを外してもなお維持できたのは大きい。②**呼値の内訳は予想と逆だった。** 同じ銘柄・同じ日を通常銘柄の呼値（1円）で評価し直すと、コストは4.75bps → **21.09bps** に跳ね上がり、net は 0.5%→-11.51 / 1.0%→-5.95 / 1.5%→-0.47 / 2.0%→**+3.12bps**。**細かい呼値がなければ、最良の2.0%バケットが辛うじて正になるだけ**で、取引可能な優位ではない。事前に立てた「右端が正なら本質は資金の問題」という読みは外れ、**TOPIX100 であること（0.1〜0.5円の呼値）は付随条件ではなく必要条件**だった。③したがって**要件は2つが同時にかかる**: (a) TOPIX100 構成銘柄であること、(b) その銘柄（株価1,250円超が99銘柄中92）を建てられるだけの資金があること。(a) は市場が決めるので動かせず、(b) だけが人間の判断で動かせる変数として残った （資金スイープ: 80万→18銘柄 / 120万→35 / 200万→62・意思決定ログ65） |
| 68 | **資金判断の材料が bps のままでは足りないので、建玉シミュレーション（`--capital` 相当の資金スイープ）を `measure_gap_fade.py` に追加した。同時に買建／売建も分けた** | これまでの表は **bps（1回の取引の取り分）までしか出していない**。だが月利は `1回の取り分 × 1日に建てられる回数 × 資金のうち建玉になる割合` で決まり、後ろ2つは資金額に強く依存する——資金が増えると株価上限（`資金 × 25% ÷ 100株`）が上がって候補が増え、同時保有5枠が埋まりやすくなる。**「いくら入れるか」は人間が判断すること（`CLAUDE.md`）だが、判断材料が bps だけでは決められない。** `capacity_stats` を追加し、日ごとに |ギャップ| の大きい順で枠を埋めるシミュレーションを行う。**安全装置をバイパスしない**: 1銘柄 ≤ 資金25%（#7）・同時保有 ≤ 5（#7）・建玉総額 ≤ 現金（レバ1倍）・単元株100株単位。**これは上限見積りであってバックテストではない**——損切り／利確・ブレーカー三層（#4〜6）・5分足の約定モデル・売建の在庫確認をどれも含まず、すべて成績を良い側に倒す単純化なので、**ここで月利が目標に届かなければ、実装すれば必ず下回る**。あわせて買建（ギャップダウンのフェード）と売建（ギャップアップのフェード）を分けた——**売建には安全装置#3（ストップ必須）と一般信用の売建可能銘柄リスト（Stage A では取得不能）という、買建にない制約が乗る**ため、買建だけで成立するかどうかで実装の難易度が変わる。ただし方向で分けた分だけ比較の数が増えているので、**片方が良く見えてもそれだけで方向を絞らない**（意思決定ログ45の多重比較補正と同じ問題） |
| 69 | **建玉シミュレーションの実測: 上限見積りでも月利は最大 +3.91%（資金200万円）で、目標+5%に届かない。しかも300万円では減る** | 実測（TOPIX100・ギャップ下限2.0%・同時5銘柄・1銘柄25%・レバ1倍）: 50万円→枠0.87/日・建玉率17.1%・**+1.28%** / 80万円→1.73・34.6%・**+2.57%** / 120万円→2.49・47.7%・**+3.69%** / 200万円→3.21・62.1%・**+3.91%** / 300万円→3.42・66.6%・**+2.81%**。**損切り・ブレーカー三層・5分足の約定モデル・売建の在庫確認をどれも含まない上限見積りでこの水準**なので、事前に登録したとおり「実装すれば必ず下回る」に該当する。**300万円で減っているのは注目に値する**——建玉率は上がっている（62.1→66.6%）のに月利が落ちる、つまり資金を増やして買えるようになった銘柄のほうが成績が悪い。1回あたりの取り分を逆算すると 50万→37.4bps / 80万→37.1 / 120万→38.7 / 200万→31.5 / 300万→21.1bps で、**120万円あたりまでは安定し、その先で急落する**。ただし**この山を「最適資金」として採用しない**——手元データへの当てはめであり、`--stress-test` で95.5%を棄却した規律と同じ理由で、曲線の最大値を後から選ぶことはしない |
| 70 | **方向別に決定的な非対称が出た（買建 +35.95bps vs 売建 +4.99bps）。だが相場の上昇ドリフトで説明できる疑いがあるので、市場ドリフトを控除した診断を先に行う** | 実測: 2.0%バケットで買建2,705件 **+35.95bps** / 売建3,081件 **+4.99bps**。**買建は閾値とともに単調に伸び**（6.43→15.75→26.63→35.95）、**売建はほぼ横ばいで非単調**（4.70→6.80→7.51→4.99、1.5%が頂点）。買建は安全装置#3（ショートのストップ必須）も売建可能銘柄リスト（Stage A では取得不能）も要らない、実装が楽な側でもある。**しかし疑うべきはこの非対称の正体**——`fade_score` はギャップダウン日には `始値→終値` そのもの、ギャップアップ日にはその符号反転なので、**日中に平均して上昇するドリフトがあれば買建だけが自動的に得をする**。全日ベースラインが +5.39bps と正なのはまさにその兆候で、検証期間（2024-08〜2026-08）が日本株の上昇局面だったなら、測っているのは「ギャップのフェード」ではなく単に「大型株を寄りで買って引けで売る」という相場βかもしれない。それなら +35.95bps は α ではなく、相場が変われば消える。`demean_by_day` を追加し、**その日の全銘柄の平均 `始値→終値` を各銘柄から差し引いて測り直す**（`market_drift_bps` でドリフト量そのものも出す）。**判定基準を結果を見る前に固定した**: ①控除しても買建の優位がおおむね残り閾値との単調性も保たれる→銘柄固有の効果なので買建のみの建玉シミュレーションに進む、②控除で買建が売建の水準まで落ちる→非対称の正体は相場ドリフトであり、「ギャップ・フェード」という枠組み自体が誤りで上昇局面を測っていただけ。**この控除は事後診断であって実戦の変種ではない**（その日の市場平均は引けまで分からない）。`regime` の calm/wild 分類と同じ位置づけで、説明には使えるがエントリー判断には使えない。あわせて買建のみの資金スイープも追加したが、**方向を絞るとその方向のシグナルが1件も出ない日が黙って分母から消える**ため、`universe_days` で営業日の分母を外から与えるようにした（分母が縮むと月利が過大に出る） |
| 71 | **市場ドリフトを控除すると買建の優位が86%消えた（+35.95→+4.94bps）。事前登録した基準どおりギャップ・フェードを棄却。ただし「上昇局面だった」という当初の読みとは中身が違った** | 実測（控除後の net）: 買建 0.5%→-0.93 / 1.0%→+1.92 / 1.5%→+4.93 / 2.0%→**+4.94bps**、売建 -1.48 / -0.56 / -1.21 / -4.12bps。買建と売建の差は 30.96 → **9.06bps** に縮んだ。**事前登録した②「控除で買建が売建の水準まで落ちる」に該当する**ので、非対称の主因は銘柄固有の平均回帰ではなく市場全体の動きだった。**ただし当初の仮説（検証期間が上昇局面だった）は外れている**——全ペアの平均 `始値→終値` は **-1.15bps とわずかに負**で、無条件の上昇ドリフトはない。消えたのは**条件付きの**市場の動きで、逆算すると**ある銘柄が2%以上ギャップダウンした日、市場全体がその日およそ31bps 上昇して引けている**（海外市場の急落を受けて寄り安→日中に戻す、という日本市場でよくある形）。**これは実在する現象だが、性質がまったく違う**——銘柄固有の平均回帰ではなく**市場全体の値動きへの方向性の賭け**であり、レバレッジ1倍・安全装置16項目で市場中立に近い設計を積んできた本システムとは別の商品になる。しかも建玉シミュレーションは控除前の数字でも上限見積り最大+3.91%（意思決定ログ69）で目標+5%に届いておらず、控除後の +4.94bps に引き直すと月利は 0.4%程度にしかならない。**ギャップ・フェードは棄却。** TOPIX100 の細かい呼値がコストを1桁下げるという発見（意思決定ログ61〜67）自体は有効で残るが、それを乗せるだけの優位が見つかっていない |
| 72 | **t値を件数ベースから日クラスタに直した（記録済みの t=8.0〜10.1 は過大だった）** | 意思決定ログ66・67で t値8.0〜10.1 を「統計的に本物」の根拠として記録したが、**これは5,786件を独立な観測として計算したもの**だった。意思決定ログ71で「同じ日の銘柄は市場要因で強く相関している」ことが実測で示された以上、**独立な観測は件数ではなく日数ぶんしかない**——ある日に市場全体が上げればその日のギャップダウン銘柄はまとめてフェード側に振れる。`clustered_stats` を追加し、日ごとに平均してから日をまたいで t値を出すようにした（日ごとに等ウェイトになるので点推定もわずかに変わるが、それは意図した挙動——該当銘柄が多い日を過大に扱わない）。出力の主表に「日数」と「t値(日)」を並べ、**見るべきは t値(日) のほう**だと明記した。**これは新しい探索ではなく、記録済みの数字の訂正**——`fill_price()` の欠陥（意思決定ログ34）やCorwin-Schultz の切り捨てバイアス（意思決定ログ59）と同じ種類の、測定そのものの誤りを見つけて直す作業に当たる |
| 73 | **【人間の決定】パフォーマンス目標を月利5%（年利79.6%）から年利25〜35%へ変更** | ユーザーの判断（2026-08-27）: 「月利3%でも年利に直すと40%近くになり非常に良い成績。年利で25〜35%程度を目標にしてほしい」。**これは合格基準の変更であり、`CLAUDE.md`「人間が判断すること」に明示的に列挙されている項目**なので、エージェントの判断ではなく人間の決定として記録する。換算: 年利25% = 月利1.88% = 1日平均0.093%（50万円で日次465円）、年利35% = 月利2.53% = 0.125%（626円）。旧目標の月利5%は年利79.6%、ストレッチの月利10%は年利213.8%で、**月利ベースで考えると小さく見える数字が複利で年に効くと非現実的な水準になっていた**。**副作用として当日決済漏れの重みが増した**——強制決済1回2,200円は、旧目標では日次1,220円の約2日分だったが、新目標では日次465円の**約5日分**になる。`docs/02` `docs/05` `docs/07` `docs/09` `config/risk.yaml` `execution/close_all.py` の該当記述をすべて更新した。**合格の門は依然としてリターンではなくシャープレシオ**（`docs/07` §2・意思決定ログ9）であり、そこは変えていない。なお `analyze_expectancy.py` の実測では、**損益分岐(40.5%)から年利+25%まではわずか0.2ポイント**（旧目標では0.6ポイント）。実測勝率33.3%から損益分岐までが7.2ポイントなので、**壁は目標ではなく損益分岐そのもの**であり、目標を下げても竹の棄却は変わらない |
| 74 | **目標変更で覆るのは TOPIX100 ギャップ・フェードの棄却理由の片方だけ。残る理由を検証するため、期間分割・日次リターン分布（シャープ／ブレーカー発動日／最大DD）を追加** | **基準が動いた後に棄却済みの仮説を見直すのは、過学習を生む典型的な手順**なので、何が覆り何が覆らないかを先に切り分けた。**覆らないもの**: 竹（gross が負なのでどんな目標でも負け）、VWAP乖離1.5%（ランダム検定95.5% < 必要99% は成績ではなく信号の質の基準）、ギャップ・フェード一般ユニバース（全バケット net 負）。**覆るもの**: TOPIX100 ギャップ・フェードの棄却理由①「上限見積り月利+3.91% < 目標+5%」——+3.91%/月は**年利58.6%**で新目標を大きく超える。80万円の+2.57%/月でも年利35.6%、買建のみでも120万円で年利29.2%・200万円で36.7%と新目標の帯に入る。**覆らないもの**: 棄却理由②「市場ドリフト控除で優位の86%が消える」（意思決定ログ71）は目標と無関係。**ただし②だけで棄却を維持するには検証が足りていない**——ギャップ・フェードには①日クラスタ t値（意思決定ログ72で実装済み・未実行）、②期間分割（竹には `--stress-test` でかけたが未適用）、③日次リターンの分布 がまだかかっていない。**③が特に重要**: 損益の86%が市場全体の動きということは**5銘柄が同時に勝ち同時に負ける**ことを意味し、日次-2%ブレーカー（安全装置#4）の設計前提と正面から衝突する。`CapacityStats` に `sharpe` / `breaker_days` / `worst_day_pct` / `max_drawdown_pct` / `annual_return_pct` を追加し、`split_by_period` で前半・後半に二分する診断を足した。**判定基準を結果を見る前に固定**: 期間分割は「両期間とも net 正 かつ 閾値との単調性が両方で保たれる」なら通過、「片方だけ正 または 符号が入れ替わる」なら棄却。ブレーカー発動日が多ければ、リターンが目標を超えていても**安全装置と両立しない**ので採用しない |
| 75 | **3検定の実測。日クラスタ t値で 2.0%バケットが t=0.6 と崩れた——資金スイープはすべてこの閾値で計算しており、土台が抜けた** | ①**日クラスタ t値**: 0.5%→t(件)9.7/t(日)**3.0**（486日）、1.0%→10.1/**3.3**、1.5%→9.6/**2.4**、2.0%→8.0/**0.6**（456日）。**順序が逆転している**——net bps が最大の 2.0% が最も弱く、最も強いのは 1.0%。件数ベースでは8〜10だった t値が日クラスタで 0.6〜3.3 に落ちたので、意思決定ログ72の「t値(件) は過大」という診断は実測で裏づけられた。**問題は `CAPACITY_THRESHOLD_PCT = 0.020` で資金スイープを計算していたこと**——年利+58.5%・Sharpe 2.77 はすべて t=0.6 のバケットの上に乗っている。この 2.0% は「net bps が最大だったから」という**結果を見てから選んだ基準**であり、資金曲線の最大値（200万円）を最適資金として採用しなかったのと同じ理由で採用できない。②**期間分割**: 前半（243営業日）+8.90/+16.65/+23.97/+28.89bps（t日 3.0/3.5/3.0/1.7）、後半（243営業日）+2.32/+5.66/+9.83/+12.20bps（t日 1.4/1.2/0.3/**-0.6**）。**事前登録した基準（両期間とも net 正 かつ 単調性）は文面どおり通過**しているが、**後半は前半の42%まで減衰し、t値はどれも有意でない**。2.0%の後半は net +12.20bps なのにt(日) が -0.6 ——**典型的な日は負けており、該当銘柄が多い少数の日が平均を持ち上げている**。③**リスク指標**: Sharpe 1.81〜2.94、BRK（日次-2%到達）は486日中1〜19日。**「損益の86%が市場全体の動きなら5銘柄が同時に負けて運用不能になる」という懸念は外れた**（意思決定ログ74の③）——ブレーカー発動は120万円で4日/486日にとどまる。ただし**300万円では最大DD -19.6% が安全装置#6（累積DD -15%）を突破**しており、資金を増やすほどリスクが増える構造（120万円で-6.8%、200万円で-13.8%）。**結論: 棄却でも合格でもなく、測り方に欠陥があった。** 閾値を net 最大ではなく頑健性で選び直し、後半だけでも目標に届くかを実測する（`_report_threshold_robustness`）。**これは新しい探索ではない**——新しい変種を足していないので多重比較の分母は増えず、同じ1つの仮説を選択の自由度を変えて測り直しているだけ |
| 76 | **後半の格子: 閾値には耐えたが資金には耐えなかった。50万円では後半に一度も目標に届かず、閾値で符号すら変わる。資金判断はまだ行わない** | 実測（後半243営業日・年利/シャープ）: 50万円 -12.3/-6.6/-2.8/+8.1%、80万円 +9.1/+10.0/+11.7/+17.9%、120万円 +41.5/+40.8/+32.0/+43.0%、200万円 +36.9/+36.9/+38.4/+37.6%、300万円 +5.8/+5.6/+9.4/+11.0%（0.5/1.0/1.5/2.0%の順）。**事前登録した「閾値を変えても崩れない」は120万・200万では満たしている**——特に200万は4閾値で36.9〜38.4%と1.5ポイントしか動かず、このプロジェクトが出した中で最も安定した結果。**だが資金軸で壊れている**: 80万✗ / 120万○ / 200万○ / 300万✗ という「真ん中だけ効く」形で、しかも**全期間の山は200万、後半の山は120万**と山の位置まで動く。ここで120万や200万を選ぶのは**資金曲線の最大値を後から選ぶこと**であり、意思決定ログ69で「採用しない」と既に登録済みの手順に該当する。**最も決定的なのは、現在の資金50万円では後半に一度も届かず閾値で符号が反転すること**。加えて**数え方の問題**: 閾値4 × 資金5 × 期間3 × 方向2 で約120セルを見ており、その中に目標超えがあること自体は、5変種に99%補正を要求した規律（意思決定ログ45）からすると証拠にならない。**結論: 資金判断はまだ行わない。** 上限見積り（損切りもブレーカーも5分足の約定モデルも含まない）でこの結果である以上、次にやるべきは資金を増やすことではなく、**実際の約定モデルで検証すること** |
| 77 | **5分足を貯めている銘柄群が間違っていた。`fetch_bars.py` に TOPIX100 を追加（yfinance は58日しか遡れないので、取らなかった週は永久に失われる）** | `fetch_bars.py` が5分足を取っていたのは `universe.json` の Layer 1 通過133銘柄（プライム・貸借・株価上限1,250円の小型〜中型株）だけ。**この銘柄群では竹・VWAP乖離・ギャップ・フェードの3つがすべて棄却されている**（意思決定ログ46・52・56）。一方、唯一 net 正が出た TOPIX100（意思決定ログ66・67）の5分足は**1本も貯まっていなかった**。つまり「80営業日たまったら Phase 4」という計画は、**棄却済みの銘柄群のデータを貯め続ける計画**になっていた。`load_topix100()` を追加し、`measure_topix100.py` が作るキャッシュ（検証期間開始時点の一覧を優先、無ければ現在の一覧）から TOPIX100 を読んで収集対象に merge する。**サバイバーシップはここでは問題にならない**——過去の成績を測るのではなく、これから貯める銘柄を決めるだけなので。**緊急性がある**: yfinance の5分足は58日ぶんしか返さない（`MAX_LOOKBACK_DAYS`）ので、**取らなかった週のデータは二度と手に入らない**。副次的に、`load_universe` が `scale_category` を読み落としていたのも見つかった（`save_universe` は書いていた）——キャッシュ経由だと TOPIX100 判定が黙って落ちる形で、`scripts` を mypy 対象に入れたときに見つかった4件（意思決定ログ62）と同じ種類の、静かに間違う欠陥 |
| 78 | **蓄積状況を全銘柄の和集合で数えていた。1銘柄でも80日あれば「80日（227銘柄）」と出る形で、Phase 4 の開始判定が壊れていた** | `_print_accumulation` は全銘柄の日付を1つの `set` に入れて数えていたので、**銘柄ごとの差が完全に潰れていた**。TOPIX100 の94銘柄を途中から収集対象に加えた（意思決定ログ77）ことで、この欠陥が実害を持つようになった——新規銘柄が1日ぶんしか持っていなくても、既存銘柄の日数がそのまま表示される。**「データがない」を「揃っている」と読む**という、意思決定ログ54（日足の浅さを見逃した）・63（日足未取得を条件で落ちたと数えた）で二度踏んだのと同じ種類の欠陥。銘柄ごとに数え、**中央値・最小・最大**と「80日以上ある銘柄数」を出すようにした。**Phase 4 の開始判定も変えた**——「和集合が80日」ではなく「80日以上ある銘柄が日次の監視枠（`DEFAULT_MAX_WATCHLIST` = 50）を満たす」。日数だけあっても選定できる銘柄が50に届かなければ Layer 2 が回らない。実測（2026-08-27）は 227銘柄中226銘柄に5分足があり、42営業日 |
| 79 | **5分足が80営業日たまるのを待つ間に、日足で測ってきた前提そのものを検証する（`measure_intraday_fill.py`）。特に「大引けで手仕舞う」という仮定は安全装置#2 と矛盾している** | **これまで日足で測った実験はすべて「始値で建てて大引けで手仕舞う」という仮定に乗っている。**だが実際の運用は2箇所でずれる: ①エントリーは日足の始値ではなく最初の5分足、②クローズは大引け(15:00)ではなく**14:50**（`MarketScheduler.CLOSE_ALL_TIME`・安全装置#2）。**14:50 は動かせない**——閉じ損ねると翌営業日に強制決済され1注文2,200円（目標リターンの約5日分・意思決定ログ73）。つまり**大引け前10分の値動きはそもそも取りに行けない**。引けの板寄せは平均回帰が完成しやすい場なので、もし優位がそこに集中しているなら**日足で測った数字はまるごと幻**ということになる。**80営業日を待つ必要があるのは戦略の検証（ウォークフォワード）であって、仮定のズレの測定ではない。**ズレは平均の差であり、42営業日 × 226銘柄で十分に測れる。むしろ**戦略を実装する前に測るべき**——ズレが大きいなら、実装しても意味がないことが先に分かる。測るのは3つ: ①日足の始値 == 最初の5分足の始値 か（**ずれていればこれまでの `gap_pct` の計算そのものが疑わしい**）、②始値→14:50 と 始値→大引け の差、③ギャップ該当日に限った同じ差。**閾値は 1.0% を使う**——日クラスタ t値が最も高かったバケット（意思決定ログ75）であり、net bps が最大だった 2.0%（t=0.6）は結果を見てから選んだ基準だった。集計は日クラスタ（意思決定ログ72）。**判定基準を先に固定**: 14:50 と大引けで**大きく違うなら**日足で測った優位の一部はそもそも取りに行けないので前提を作り直す、**ほぼ同じなら** 14:50 の制約は無害で、日足の実験結果はそのまま使える |
| 80 | **実測: 日足の始値と最初の5分足の始値が7割の日で一致しなかった（ずれの中央値26.77bps）。追いかけてきた優位より大きいので、原因を特定するまでどの数字も信用できない** | 実測（9,287件 / 42営業日 / 235銘柄）: **一致（0.5bps未満）はわずか27.7%**、ずれの中央値 **26.77bps**、最大 608.70bps。**これまで見つけた優位は0〜24bps**なので、この誤差はそれより大きい。ギャップは「前日終値 vs 当日始値」で定義しており、その始値を日足から取っている以上、**`gap_pct` の計算そのものが疑わしくなる**。**原因は2つ考えられ、対処がまったく違う**: ①最初の5分足が寄り付きのバーではない（9:00 のバーが欠けている）→ 日足の始値は正しく、5分足側の欠損。②9:00 のバーはあるのに値がずれる → 日足と5分足で価格の基準が違う（調整の差など）で、**この場合は日足ベースの実験すべてが疑わしくなる**。最初のバーの時刻分布と、寄り付きのバーがある日／ない日で分けたずれの中央値を出す診断を追加した。**14:50 と大引けの差はほぼ無かった**（+0.80bps・t=0.3）ので、安全装置#2 による切り落としそのものは無害という見込み——ただし①の切り分けが済むまで確定させない |
| 81 | **カットオフの実装に誤りがあった。5分足のラベルは区間の開始時刻なので、`timestamp <= 14:50` は5分ぶん先読みしていた** | yfinance の5分足は区間の**開始**時刻でラベルされる。つまり `14:50` のバーは 14:50〜14:55 を表し、その終値は **14:55 の価格**。`timestamp <= 14:50` で拾うと、**14:55 まで持ち越した価格で手仕舞ったこと**になる。`LAST_BAR_START`（= 14:45）を導入し、**区間が 14:50 までに終わるバー**だけを使うようにした。規約「検証できないものは保守的な側に倒す」——もしラベルが区間終了だった場合、この選び方は1本ぶん損をするだけで、優位を水増しはしない。**ルックアヘッドは「注意して書く」では防げない**（規約7）という原則が、バーのラベル解釈という思わぬところで効いた例。あわせてセクション3を TOPIX100 / Layer 1 に分けた——net 正が出たのは TOPIX100 だけ（意思決定ログ67）で、Layer 1 では3手法とも棄却されているので、混ぜると比較にならない |
| 82 | **始値のずれの原因が確定: 日足は正しく、5分足側の欠損だった（9:00 のバーがある日はずれ0.00bps）。だが寄り付きのバーが64%の日で欠けており、5分足での検証計画を直撃する** | 実測: 最初のバーが **09:00 の日は3,304件で一致62.3%・ずれの中央値0.00bps**、**09:05 の日は5,983件で一致8.6%・ずれの中央値39.42bps**。**意思決定ログ80で立てた①（5分足側の欠損）が正しかった。** 9:00 のバーがある日は完全に一致するので、**日足の始値は信用してよく、これまでの `gap_pct` の計算も正しい**——日足ベースの実験は救われた。**だが新しい問題が出た**: yfinance が 9:00〜9:05 のバーを**63.8%の日で返していない**。日足の実験には影響しないが、**寄り付きで建てる戦略の5分足検証を直撃する**——9:00 のバーが無ければ 9:05 で建てるしかなく、その時点で価格は中央値39bps動いている。**追いかけている優位（0〜24bps）より大きい。** 最後のバーも 15:20 が96.7%で、東証が2024年11月から15:30引けになっていることを考えると引け際も欠けている疑いがある。**8週間後ではなく今知る必要がある**——貯めているデータが使えないなら貯め方を変えないといけない。`_report_completeness` を追加し、①1日あたりのバー本数（完全なら66本）、②9:00 バーの有無を日付の前半/後半で比較（**Yahoo が古い日を間引いているか**）、③銘柄ごとの分布（**流動性の問題か**）を出す。日付で決まるなら週1回の取得では取りこぼしを埋められないので頻度を上げる必要があり、銘柄で決まるならその銘柄を検証から外せばよい。なおカットオフ修正後の 14:50→大引けは **+2.48bps（t=0.8）**（修正前は+0.80bps）で、依然として有意ではない |
| 83 | **欠損の原因は日付でも銘柄でもなかった。Yahoo の日本株5分足は寄り付きの1本を構造的に落とす。決めるべきは「欠損をどう埋めるか」ではなく「9:05 で建てても優位が残るか」に変わった** | 完全性の実測: **1日あたりの本数は中央値65本**（完全なら66本）で、**セッション全体はほぼ揃っており欠けているのは寄り付きの1本だけ**。事前に立てた2つの仮説はどちらも否定された——**日付**: 前半37.6% vs 後半33.7% で差がなく、Yahoo が古い日中データを間引いているのではない。**銘柄**: 0〜25%が56銘柄・25〜50%が125・50〜75%が42・75〜100%が3 と一様に散らばり、流動性で二分されるわけでもない。**ラベルの向きも確定した**: もしラベルが区間終了なら「09:05」のバーは 9:00〜9:05 を表し、その始値は寄り付き値になって日足と一致するはず。**一致しない（39bps ずれる）以上、ラベルは区間開始で、9:00〜9:05 のバーは本当に存在しない**（意思決定ログ81の保守的な選択が結果的に正しかった）。**データ源を変えない限り取れない**ので、問うべきは「9:05 で建てても優位が残るか」になる。`fade_bps(entry_at_first_bar=True)` を追加し、**実際に建てられる最初のバーの始値**で建てた場合を並べた。**`gap_pct` は日足の始値のまま**——シグナルの定義は「前日終値 vs 当日始値」で実測でも正しい（意思決定ログ82）ので、変えるのは建てられる価格だけ。あわせて `MIN_BARS_FOR_ANALYSIS`（50本）で、バーが1本しか無いような日を集計から外した——そういう日の「最初のバー」は寄り付きと無関係な時刻になり、測定が壊れる。**判定基準を先に固定**: 「実現可能」の行が従来の仮定とほぼ同じなら寄り付きの欠損は無害で戦略を9:05エントリーとして設計すればよい、**大きく落ちるなら TOPIX100 ギャップ・フェードは実務上の理由で死ぬ**（優位はあるが取りに行けない） |
| 84 | **TOPIX100 ギャップ・フェードを棄却。死因は 14:50 ではなく寄り付き直後の数分だった——5分遅れて建てるだけで19〜26bps 失う** | 実測（ギャップ1.0%以上・42営業日）: 全銘柄で 日足の始値→大引け **+1.56bps**（t=0.1）/ 日足の始値→14:50 **-0.42bps**（t=-0.0）/ **最初のバー→14:50 -25.99bps（t=-2.5）**。TOPIX100 では +2.53 / -0.19 / **-18.93bps（t=-1.7）**。**「実現可能」の行だけが表の中で唯一 t値が有意に触れている。**事前登録した基準（「大きく落ちるなら実務上の理由で死ぬ」）に該当する。**事前に疑っていた「引け際に優位が集中している」は外れた**——14:50 の影響は +2.48bps で無害（意思決定ログ82）。優位は**寄り付き直後の数分**に集中していた。**ここに構造的な問題がある**: ギャップは `(当日始値 - 前日終値) / 前日終値` なので、**始値が付いて初めてシグナルが確定する**。つまり寄成注文（板寄せに参加する注文）は使えず、最速でも「始値を見てから成行を出す」形になる。5分後には優位が消えている以上、**これは秒単位の執行速度に成否がかかる戦略**であり、意思決定ログ13（板を見る手法ほどレイテンシ勝負になり個人が不利。だから竹は板をシグナルに使わない）と同じ性質のものになる。**ただし断定はしない**——実運用（kabuステーションAPI）なら 9:00:01〜9:00:05 で約定できる可能性があり、「5分遅れ」と「5秒遅れ」は別物。**だが Stage A に秒単位の分解能を持つデータ源は無く、検証する手段が存在しない。**事前登録どおり棄却しつつ、理由を「優位が無い」ではなく**「優位はあるが、この設計では取りに行けず、取りに行けるかを確かめる手段も無い」**として記録する |
| 85 | **減衰カーブ（0/5/15/30/60分）を測る道具を残す。「数分の遅れに耐えるか」を次の手法探しの設計制約にする** | ギャップ・フェードが死んだ理由は**ギャップ固有ではなく、優位が寄り付き直後に集中していたこと**。同じ形の手法はすべて同じ理由で死ぬので、**候補を思いつくたびに最初に確かめるべき性質**になった。`fade_with_delay_bps` と `_report_decay` を追加し、エントリーを 0/5/15/30/60分 遅らせたときの優位を並べる。遅延0は日足の始値（板寄せの約定値。5分足に9:00のバーが無くても実在する価格で、意思決定ログ82で0.00bps一致と確認済み）、遅延Nは **9:00 + N分 以降に始まる**最初のバーの始値——「以降」にするのは、その時点でまだ始まっていないバーの価格を使わないため（規約7）。**この診断はギャップに依存しない**ので、次に試す手法にもそのまま当てられる。**判定の考え方**: 落ち方が速い手法は秒単位の執行速度に成否がかかり、意思決定ログ13 の方針から外れる。**優位があっても採らない**——個人が構造的に不利な土俵で戦わない、という設計判断は変えない |
| 86 | **減衰カーブの実測は「なだらかな減衰」ではなく「崖」だった。意思決定ログ85で書いた設計原則（落ち方が速い手法は採らない）を訂正する** | 実測（TOPIX100）: 0分 **-0.2bps** → 5分 **-18.7bps** → 15分 -22.1 → 30分 -13.7 → 60分 -13.0。**5分で崖のように落ちた後は横ばい**（15〜60分は -13〜-22 でほぼ動かない）。逆算すると **始値→5分 で約19bps フェード方向に動き、5分→14:50 でほぼ同じだけ戻している**（この42営業日の窓では日中トータルがほぼゼロ）。**これは板寄せのオーバーシュートと整合する**——板寄せは単一価格で約定するので、その直後に実勢へ寄る。**ギャップ・フェードが日足で捉えていた優位の正体は、その最初の数分の補正だった。****したがって「遅延そのもの」は問題ではない**——5分でも60分でも成績は変わらない。問題は**「板寄せの約定値を取れるか」の一点**。意思決定ログ85に書いた「落ち方が速い手法は、優位があっても採らない」は**一般化として不正確だったので訂正する**。**正しい設計原則**: シグナルが**前日引けまでの情報だけで確定する**なら寄成注文が使えて板寄せの価格を取れる。**当日の始値を必要とするシグナルは循環していて取れない**（ギャップは定義上その日の始値で決まる）。**なお寄り前気配を使えば近似できる**——Stage A では取得できないが **Stage B（kabuステーションAPI）では取れる**（`docs/03-universe.md`）。つまりギャップ・フェードは「永久に死んだ」のではなく**「Stage A では検証も実行もできない」**が正確な状態。ただし寄り前気配 ≠ 始値なのでシグナルは確実に劣化し、どれだけ劣化するかは Stage B のデータが無いと測れない |
| 87 | **【事前登録】前日リターンの反転仮説。意思決定ログ86の設計原則（シグナルが前日引けで確定するなら寄成で板寄せの価格を取れる）を満たす初めての候補** | **仮説**: 前日に大きく動いた銘柄は、翌日の板寄せで行き過ぎ、寄り付き後に戻す。**シグナル** = `(前日終値 - 前々日終値) / 前々日終値`（**前日大引け時点で確定**）、**エントリー** = 寄成（＝当日始値）、**手仕舞い** = 大引け（14:50 との差は +2.48bps で無害と実測済み・意思決定ログ82）、**スコア** = `-sign(前日リターン) × (当日始値→終値)`。**ギャップとの決定的な違いは、当日の始値を参照しないこと**——ギャップは `(当日始値 - 前日終値) / 前日終値` なので、シグナルが確定する時点で既に板寄せが終わっており、寄成注文に間に合わなかった（意思決定ログ84・86）。今回はそこを回避している。**判定基準を結果を見る前に固定し、コードに埋め込んだ**（`_report_verdict`）: ①`|前日リターン|` の下限バケット（1/2/3/4%）で **net(日) が単調に改善する**、②どこかのバケットで **日クラスタ t値 >= 2 かつ net(日) > 0**、③**前半・後半とも最大バケットで net(日) > 0**。**3つとも通って初めて5分足での検証に進む。どれか1つでも外せば棄却**——95.5%という僅差を棄却した規律（意思決定ログ46）と同じ扱いにする。判定をコードに埋め込んだのは、**結果を見てから基準を動かさないため**。なお短期反転は学術的に広く知られた効果であり、新規性は目的ではない——**このプロジェクトのコスト構造と安全装置の下で生き残るか**だけが問題。あわせて日クラスタ統計と期間分割を `src/autotrader/diagnostics.py` に出した——`measure_gap_fade.py` と `measure_intraday_fill.py` が同じものを別々に持っており、3つ目の診断でも要るので、規約「同じことをする関数を二つ作らない」に従った |
| 88 | **前日リターンの反転も事前登録どおり棄却（3条件とも×）。5家族すべてで「必要14〜20bps に対し実測0〜5bps」という同じ形が出た** | 実測（TOPIX100・486営業日・48,107件）: gross +2.45 / +4.97 / +4.09 / +4.47bps に対しコスト約4.8bps で **net はほぼゼロ**（-2.41 / +0.20 / -0.62 / -0.19）。日クラスタ t値は 0.7 / 1.1 / -0.1 / -0.7 で有意でなく、単調性もない。**前半は全バケットで負**（-5.18 / -5.36 / -13.60 / -19.09bps）で、後半だけが正。事前登録した3条件すべて×。Layer 1（133銘柄）は gross が -0.12〜-2.29bps と**コスト前で既に負**なので議論の余地がない。**5家族を並べると同じ形をしている**: 竹 -0.3bps / VWAP乖離1.5% +10bps / ギャップ一般 +15.7bps / ギャップTOPIX100 +24.2bps（うち86%は市場β・かつ執行不能）/ 前日反転 +5.0bps。**市場要因と執行制約を除くと、どれも 0〜5bps に落ち着く。**そこで**毎回逆算せずに済むよう** `diagnostics.required_gross_bps` を追加した——目標年利・コスト・建玉率から必要な gross を導く**算術**（基準ではない）。年利25%なら TOPIX100（コスト4.8bps）で**建玉率100%なら14.1bps・実測の建玉率62%なら19.8bps**、通常銘柄（21bps）なら30.3bps・36.0bps。**探しているのは14〜20bps、見つかるのは0〜5bps。** この3〜4倍の開きが5家族すべてで一貫している。個々のシグナルの当たり外れではなく、**探索の設計そのものを問い直す段階**——残る未検証領域は①十分な母数の日中構造（5分足80営業日・収集中）、②保有期間の変更（当日決済必須という前提そのもの）、③Stage B のデータ（寄り前気配・板）。②③は `CLAUDE.md`「人間が判断すること」に属する |
