# CLAUDE.md

日本株の自動売買ツール。資金50万円、三菱UFJ eスマート証券の kabuステーションAPI で発注する。

**このプロジェクトは誤りが金銭損失に直結する。** 一般的なソフトウェアと違い、
バグは「動かない」では済まず、意図しない発注や建玉の閉じ忘れがそのまま現金の損失になる。

## Stage A / Stage B

工程は2段階（`docs/09-data-sources.md`）。**今は Stage A。**

| | Stage A | Stage B |
|---|---|---|
| 口座・API | **不要** | 必要（運用資金は入れない） |
| データ源 | J-Quants 無料 + yfinance | 上記 + kabuステーションAPI |
| Phase | 1〜3 | 4〜6 |
| 判定 | 戦略に見込みがあるか | システムが信頼できるか |

**Stage A では以下が使えない。** 実装時に前提を混同しないこと:

- 板情報（bid/ask/厚み）→ 約定モデルは**保守的な側に倒す**（片道20bps）
- 寄り前気配 → 選定は**前日引け時点の情報のみ**（`universe/selector.py` の `StageAFeatures`）
- 一般信用デイトレの売建可能銘柄リスト → 流動性上位で代理
- 発注系全般 → Stage B で検証する

---

## 絶対に守ること

### 1. レバレッジ1倍の不変条件

```
建玉総額（買建 + 売建の合計） ≤ 現金残高
```

**全発注は `src/autotrader/risk/leverage.py` を必ず通る。バイパス経路を書かない。**
信用取引を使いながら使用金額を現物と同水準に抑えることが、本システムの安全性の土台。

### 2. 建玉は必ず当日中に閉じる

デイトレ信用の建玉を閉じ損ねると、**翌営業日に強制決済され1注文2,200円**。
目標リターンの約5日分が消える。

`execution/close_all.py` が最重要コードパス。大引け（14:50）と緊急停止で共用する。
**クローズ後は `GET /positions` で残存を実測確認する**（成功したはずと仮定しない）。

### 3. ショート建玉はストップ注文なしで作らない

空売りは理論上損失無限大。レバレッジ1倍でもこの性質は変わらない。

### 4. 認証情報をコードに書かない

`.env`（`.gitignore` 済み）から環境変数経由でのみ読む。
`data/`（市場データ）と `KILL`（キルスイッチ）もコミットしない。

### 5. コストをゼロにしない

デイトレ信用は手数料0・金利0・貸株料0だが、
**スリッページとスプレッドは必ずモデル化する**。
コストゼロのシミュレーションを書くと本番で必ず乖離する。

**検証できないものは必ず保守的な側（成績が悪くなる側）に倒す。**
Stage A は板がないため、スリッページを Stage B より厚く見積もる（20bps vs 10bps）。
楽観的な約定モデルでバックテストに勝つのは簡単だが、それは目的ではない。

### 6. データ取得の規約（yfinance の落とし穴対策）

`data/yahoo.py` に実装済み。**新しくデータ取得を書くときも必ず守る**
（詳細は `docs/09-data-sources.md` §2.5）。

- **空の DataFrame を握り潰さない。** yfinance はブロックされても例外を投げず空を返す。
  「データなし」と「ブロック」が区別できない以上、**例外にして安全側に倒す**
- **`auto_adjust=True` を必須にする。** 未指定だと分割前後で価格が不連続になり、
  ATR% や売買代金が壊れて架空の急騰を誤検出する
- **欠損は `pd.notna()` で判定する。** `is None` では `NaN` を検出できない
- **tzキャッシュはプロセス固有ディレクトリに隔離する。** 複数プロセスが同時に触ると落ちる
- **取得可能期間の超過は黙って切り詰めずエラーにする。** 取れたつもりで欠損しているのが最悪
- **単一データソースに依存しない。** `data/base.py` の `FallbackDataSource` を使い、
  **フォールバックの発動は必ずログに残す**
- **列構造を銘柄数で仮定しない。** yfinance は単一ティッカーでも MultiIndex を返す。
  仮定すると全行が欠損扱いで捨てられ、**エラーなく空になる**（実際に踏んだ）
- **J-Quants は V2。** パス（`equities/master` / `equities/bars/daily`）も
  レスポンス構造（`{"data": [...]}`）も項目名（短縮形）も V1 から変わっている
- **レート制限はバーストせず均等間隔で送る。** スライディングウィンドウだと
  サーバ側の窓の境界で超過して 429 になる（実際に踏んだ）
- **レート制限エラーを「データなし」と混同しない。** 測定ループで混同すると
  **測定値そのものが嘘になる**（実測の遅延が84日→88日にずれた）。
  `RateLimitError` を分離してあるので、受けたら中断して「測定不能」と報告する

### 7. 未来のデータを参照しない

バックテストは**構造的に**ルックアヘッドを防ぐ。「注意して書く」では不十分。
寄り前の判断に使ってよいのは、前日大引けまでの確定情報と当日の寄り前気配だけ。

---

## ドキュメント

作業前に該当するものを読む。

| ファイル | 内容 |
|---|---|
| `docs/00-overview.md` | 全体像・工程・**意思決定ログ** |
| `docs/01-broker-api.md` | kabuステーションAPI の仕様と制約 |
| `docs/02-margin-rules.md` | 信用取引ルール・レバ1倍の不変条件・当日決済要件 |
| `docs/03-universe.md` | 銘柄選定の2層構造・プレミアム枠・バイアス回避 |
| `docs/04-strategies.md` | 竹（メイン）・松/梅（将来）の手法定義 |
| `docs/05-risk-management.md` | **安全装置16項目** |
| `docs/06-operations.md` | 運用手順・障害時対応・キルスイッチ |
| `docs/07-go-live-criteria.md` | **実弾移行の合格基準（後から緩めない）** |
| `docs/08-development-harness.md` | 開発ロールとモデル選択 |
| `docs/09-data-sources.md` | **データ源と Stage A/B の2段階構成** |

---

## 開発ハーネス

`.claude/agents/` にロールを定義してある（詳細は `docs/08-development-harness.md`）。

| ロール | モデル | 権限 | 責務 |
|---|---|---|---|
| `architect` | Opus 5 | 読み書き | 設計判断・インターフェース定義 |
| `implementer` | Sonnet 5 | 読み書き | 確定した仕様の実装 |
| `data-engineer` | Sonnet 5 | 読み書き | データ取得・整形・永続化 |
| `test-writer` | Sonnet 5 | 読み書き | テスト作成（仕様から書く） |
| `reviewer` | Opus 5 | 読み取りのみ | コードの正しさ検証 |
| `risk-auditor` | Opus 5 | 読み取りのみ | 安全装置16項目の監査 |
| `backtest-validator` | Opus 5 | 読み取りのみ | バイアス検出・過学習検証 |

**判断が要る仕事に Opus、手が要る仕事に Sonnet。**
検証系は read-only にして、指摘の独立性を保っている。

### 必ずゲートを通すタイミング

- **新しい手法の仮説を立てたとき → `docs/04-strategies.md` の
  「仮説を立てたら、測る前に執行可能性を確認する」5問**
  （データ不要。答えられないなら優位より先に執行可能性を測る）
- Phase 3 完了判定 → `risk-auditor`
- ペーパートレード開始前 → `risk-auditor`
- バックテスト結果の採用判断 → `backtest-validator`
- **実弾移行前 → `risk-auditor` と `backtest-validator` の両方**

---

## 人間が判断すること（エージェントに委ねない）

- 安全装置の閾値を緩める
- 合格基準（`docs/07-go-live-criteria.md`）を変える
- 実弾に移行する / 投入額を増やす
- ブレーカー発動後の再開承認
- 対象市場・証券会社を変える

---

## コーディング規約

- Python 3.11+ / `ruff`（line-length 100）/ `mypy --strict`
- 型注釈を必ず付ける
- 外部API のレスポンスは `pydantic` で検証する（そのまま信用しない）
- エラーを握り潰さない（`except: pass` を書かない）
- 金額計算は浮動小数点の誤差累積に注意する
- **既存の関数を探してから新規に書く**（同じことをする関数を二つ作らない）

### 実行

```bash
pip install -e ".[dev]"
ruff check .
mypy
pytest
```

---

## 現在のステータス

**Phase 1〜3 完了。Phase 4 は5分足が80営業日ぶん貯まってから。**

- Phase 1: データ基盤（`data/`）。全項目を実機で実測済み
- Phase 2: Layer 1/2（`universe/`）、バックテスト基盤（`engine/` `broker/replay.py`）
- Phase 3: 竹（`strategy/take_intraday.py`）、ブレーカー三層（`risk/limits.py`）、
  発注まわり（`execution/`）、スケジューラ（`engine/scheduler.py`）、
  日次レポート（`report/daily.py`）

### 実測で確定した値（2026-08-23 / 基準日 2026-05-29）

```
全上場 4,451 → プライム 1,565 → 貸借 1,483 → Layer 1 通過 133銘柄
  → ATR% 2〜5.33% で 106銘柄 → 日次の監視枠 50/50 が埋まる
```

| 値 | 確定値 | 由来 |
|---|---|---|
| 株価上限 | **1,250円** | 資金 × 25%(#7) ÷ 100株 |
| 通常枠の上限 | **1,000円** | 資金 ÷ max_concurrent(5) ÷ 100株 |
| 流動性下限 | **3億円** | 100銘柄を超える最も厳しい水準（実測） |
| ATR% の下限 | **2%** | 往復コスト（片道20bps×2）の5倍 |
| ATR% の上限 | **5.33%** | 日次ブレーカー(2%) ÷ (25% × 損切り1.5倍) |
| 移動窓の損失 | **直近15日で -5%** | 日次(-2%)の2.5倍・累積DD(-15%)の1/3 |

**上限比率25%は動かさない。** 日次ブレーカー(-2%)と1.5×ATR損切りからの
逆算値で、40%にすると ATR 4% の銘柄で1敗が当日を終わらせる。

### 竹の現パラメータはランダム検定で棄却された（2026-08-24）

| | トレード数 | gross/件 |
|---|---:|---:|
| **竹** | 555 | **-2.3円** |
| ランダム（20シード）中央値 | 280 | **+17.5円** |
| ランダムの範囲 | | -54.9 〜 +45.5円 |

**竹はランダム20本のうち7本しか上回らなかった（35パーセンタイル）。**
エントリーだけをランダムにして手仕舞い・制約をすべて揃えた比較なので、
**シグナルが何もしていない**ことを意味する。

```
orb              538件  勝率20.8%  gross  -10円/件   ← 負けの本体
vwap_reversion    17件  勝率41.2%  gross +250円/件   ← 唯一のプラス（n=17）
```

**順張りが効かず逆張りが効いた可能性**があるが、n=17 では偶然と区別できない。
検定は `python scripts/backtest_take.py --no-breakers --experiment`。

**39営業日は in-sample。** 変種を複数試している以上どれかは偶然勝つので
（5変種なら約23%）、勝っても仮説として記録するだけ。確認は80営業日たまってから。

### `--experiment` で ORB反転は棄却、VWAP乖離単独1.5%だけが生存（2026-08-24）

| 変種 | 件数 | gross/件 | ランダムに対する順位 | 判定 |
|---|---:|---:|---:|---|
| 竹（現状） | 555 | -2.3円 | 35% | 棄却 |
| ORB 反転 | 565 | +21.5円 | 75% | 棄却 |
| **VWAP乖離のみ 1.5%** | **221** | **+84.9円** | **95%** | **生存** |
| VWAP乖離のみ 1.0% | 434 | +4.6円 | 80% | 決定的でない |
| VWAP乖離のみ 0.7% | 571 | +20.4円 | 75% | 棄却 |

**ORB を切ると vwap_reversion が17件 → 221件に増えた。**
`max_entries_per_symbol_per_day=1` により ORB が先にその日の枠を使い切っており、
**竹の実測はほぼ ORB だけを測っていたに等しい**。

**反転仮説は否定された。** 竹35%とその逆75%はノイズを裏返しただけの対称的な結果。
**閾値を緩めると単調に悪化**（95%→80%→75%）しており、n=221が
たまたま良かった可能性を否定できない。

#### この95%は額面どおりに受け取れない

- **20シードの「95%」は「20本中19本を上回った」の意味。** 1本負けが増えれば
  90%になり、95%と80%を区別できる解像度がない
- **5変種を試した以上、単純な95%は基準として甘い。** Bonferroni補正では
  `1 − 0.05/5 = 99%` が要るが、20シードでは原理的に測れない（最高が95%）

**`--stress-test` で多重比較補正した閾値（99%）と前半/後半の期間分割を
かけるまで、この結果を採用しない。**

```
python scripts/backtest_take.py --no-breakers --disable-breakout --stress-test
```

`required_percentile()` / `required_seeds_for()` が変種数から必要な閾値と
最低シード数（5変種→99%→100シード、既定200）を導出する。前半・後半への
分割はバーを切らず監視リストだけを絞る（ATR の助走を保つため）。
**全期間が99%以上 かつ 前半・後半とも50%超 でなければ棄却**——
片方の期間だけで勝っていても偶然と区別しない。

**実データで走らせた結果、棄却が確定した**（2026-08-24）。

```
全期間 221件  95.5%（必要閾値99%に届かない）
前半   112件  66.0%
後半   112件  87.0%
```

事前登録した基準どおり棄却を確定した。ただし前半・後半どちらも
ランダムより上で、竹本体(35%)・ORB反転(75%)のような明確な負けとは
質が違う。**現行の竹（ORB+VWAP混合）は Phase 3 の設計として終了。**
次はエントリー根拠を再設計する（`docs/04-strategies.md` 冒頭の注記を参照）。

#### ユーザー提案（相場局面に応じた手法選択）への回答

「相場の局面によって効く手法は違うはず。過去10年分などから局面ごとに
何が効くか先に明らかにすべきでは」という提案があった。

**判断: 発想は妥当だが、今すぐ大規模にやるのは時期尚早。**
J-Quants無料は日足でも過去2年まで・5分足はyfinanceで過去58日までで
**10年分のデータが物理的に手に入らない**。また局面×手法の組合せは
検定すべき数を掛け算的に増やし、5変種の比較ですら99%補正が要った
現状のデータ量では検証しきれない。**どの局面でも通用する土台が
まだ1つもない**段階で使い分けを作るのは早い（意思決定ログ47）。

**縮小版として、外部データを増やさず日次の値動きの荒さでVWAP乖離の
成績を事後的に分けて見る診断を先に行う。**

```
python scripts/backtest_take.py --no-breakers --disable-breakout --by-regime
```

`src/autotrader/regime.py` が対象日を穏やか(calm)/荒れ(wild)に
中央値で二分する（当日の高安を使うため**事後診断専用**。実戦の
先読みフィルタとは別物）。calm/wildでVWAP乖離の順位がはっきり
違えば筋の通った理由があるとみなし、次に「前日までの情報だけで
予測する」先読み版の設計に進む。違わなければこの切り口は手がかりなし。

#### 初回実装は分類基準がエントリー条件と同義になっていた（実データで発覚・訂正済み）

実データで走らせたところ **221件中220件が wild、calm はわずか1件**
という、判定不能な結果になった。

原因は「その日の銘柄群を横断した中央値」を境界に**銘柄ごと**に
calm/wild を割り当てていたこと。VWAP乖離のエントリー条件
（その日その銘柄がVWAPから1.5%以上乖離＝仲間より大きく動いた）と、
旧「wild」の定義（値幅が仲間内の中央値を上回る）が**ほぼ同じものを
測っていた**ため、シグナルが発火する取引のほぼ全部が機械的に wild に
分類されていた（意思決定ログ48）。

**分類を「銘柄ごと」から「日ごと」に作り替えた。** 各日の値は
その日の全銘柄の実現レンジの中央値（＝市場全体の荒さ）とし、
**日をまたいだ中央値**で calm/wild を二分する。個別銘柄のエントリー
条件とは独立した軸になった。

#### 訂正版の実行結果（2026-08-25・実データ）

```
区分     件数    gross/件     順位
calm      72    +26.9円    73.5%
wild     149   +112.9円    99.0%
```

**今回は判定可能な形になった。** calm/wild とも判定に足る件数があり、
順位もはっきり分かれている（wild は `--stress-test` の合格ライン99%と
同水準、calm はランダムより上だが際立ってはいない）。

ただしこれは依然として①事後診断（当日の高値・安値を使うため取引前
には分からない）、②39営業日・in-sample という2つの意味で「使える
結論」ではない。順位がはっきり違うという条件は満たしたので、次は
「前日までの情報だけで今日を予測する先読み版フィルタ」の設計段階。

**ただしフィルタを設計する前に、その前提（市場の荒さは日をまたいで
持続するか）をまだ確認していなかった。** 持続しないなら、前日の情報を
どう工夫しても今日を予測する手がかりにはならない。`_regime_persistence`
（`scripts/backtest_take.py`）を追加し、`--by-regime` の出力に
「前日wild→当日wild」「前日calm→当日wild」の条件付き確率を
持続性の診断として追加した（意思決定ログ49）。新しいデータもロジックも
要らず、既存のラベルを1日ずらして突き合わせるだけ。

```
python scripts/backtest_take.py --no-breakers --disable-breakout --by-regime
```

出力末尾の「持続性」の行を見る。**はっきり違えば** 先読み版フィルタの
設計に進む価値がある。**大差なければ**、「市場全体の荒さで成績が分かれる」
こと自体は事実として記録に残すが、前日情報からの予測はできないので
この切り口はここで打ち止めにする。

### 竹の実データ検証（39営業日・パラメータ未検証）

> **この節の数値は約定モデルを固定20bpsで測ったもの。**
> コストを呼値(tick)ベースに変えたので**再ベースラインが要る**。
> 旧モデルの再現は `python scripts/backtest_take.py --flat-slippage`。

| | ブレーカー有効 | 無効 |
|---|---:|---:|
| トレード数 | 152 | 507 |
| 総リターン | **-5.10%** | **-16.00%** |

**ブレーカーが損失を1/3に抑えた。** これが Phase 3 の主目的。
移動窓(#5)が11日目に -5.01% で発動し、**-15.57%（#6 まで到達）を -5.10% に抑えた**。
成績そのものは評価しない（パラメータ未検証、39営業日は年率の15%）。

**トレード数の目標値は持たない。** 実測1日12〜13回で日次ブレーカーは0日。
縛るのは日次損益(-2%)であって回数ではない。

### 約定コストは呼値から導く（固定bpsではない）

```
往復コスト（ATR単位） = スプレッド円 ÷ ATR円
```

**株価が式に出てこない。** 効くのは「ATR が円で何円か」だけ。
600円×ATR3.33% と 2,200円×ATR0.91% はどちらも ATR 20円で同じコスト。

| | 値 | 由来 |
|---|---:|---|
| 呼値（3,000円以下・通常銘柄） | **1円** | 東証。TOPIX100 は 0.1〜0.5円 |
| スプレッド | **呼値2本** | **未実測の仮定**（`tick.DEFAULT_SPREAD_TICKS`） |
| ATR の下限 | **スプレッド円 × 5** | 往復コストが ATR の20%を超えない |
| ATR円の上限 | **66.7円** | 資金 × 日次ブレーカー ÷ (単元 × 損切倍率) |

**ATR円の上限に建玉比率が出てこない。** 同時保有数を絞ると株価上限が
上がる（tickに有利）が ATR%上限が下がる（不利）ので、正確に打ち消し合う。
**集中してもコストの理論的下限は動かない。** ただし天井に届く銘柄が
実在するかは変わるので、`scripts/measure_universe.py` §8 で実測する。

**残る最大の当て推量は「スプレッド何tick」。** Stage A では板がないので
測れない。日足OHLCからの推定（Corwin-Schultz）か、
楽天証券のマーケットスピード II RSS で板を録る（入金不要）。

### 期待パフォーマンス（`scripts/analyze_expectancy.py` で再現）

| | 値 |
|---|---:|
| 損益分岐の勝率（コスト込み） | **40.5%** |
| 実測の勝率（n=507） | **33.3%** — 95%信頼区間 [29.3%, 37.5%] |
| 年利 +25%（月利1.88%）に要る勝率 | 40.7% |
| 年利 +35%（月利2.53%）に要る勝率 | 40.8% |
| 月次換算リターン | **-8.6%** |

**信頼区間が損益分岐の外側にある = 現設定は運ではなく構造的に負けている。**
損益分岐は 損切1.5×ATR / 利確2.5×ATR / 往復40bps / ATR%中央値3.33% からの
計算値で、サンプル期間の長さに依存しない。PF 0.58 も同じことを言っている。

**ただし損益分岐から年利+25%までは0.2ポイントしかない。** 1ヶ月260回が
薄い優位を増幅する設計なので、勝率にもコスト見積りにも敏感。
**壁は目標ではなく損益分岐そのもの**——目標を下げても損益分岐は動かない。
7.2ポイントの不足をパラメータ調整で埋められるかは Phase 4 で決める。
**ここで手で合わせると in-sample の過学習になる。**

#### 負けの正体はコストではなかった（**推定が実測で覆った**）

以前ここに「コスト前は +22.5〜+34.7%。シグナルは勝っていてコストが
食っているだけ」と書いていた。**実測したら誤りだった。**

| | 推定（恒等式から） | **実測** |
|---|---:|---:|
| 払ったコスト | 資金の 38〜51% | **16.9%** |
| **コスト前リターン** | **+22.5〜+34.7%** | **-0.40%** |

推定がずれた原因は、建玉を9.5〜12.5万円と置いたが実際は**約8.5万円**
だったことと、約定モデルの欠陥で実効スリッページが設定の半分しか
出ていなかったこと（下記）。

**素の損益はほぼゼロ。gross PF は約0.99でコイン投げと区別がつかない。**
`net = gross - cost` の gross はコストモデルに依存しないので、
**この結論は約定モデルを直しても変わらない。**

コスト工学（呼値・同時保有数・ATR円スコア）はすべて二次的。
まず `scripts/backtest_take.py --no-breakers --random-baseline 20` で
**ランダムエントリーと区別できるか**を検定する。

#### 約定モデルの欠陥（2026-08-24 に発見）

`fill_price()` がスリッページを乗せた後にバーの高値/安値で頭を抑えており、
**始値=高値の足でスリッページがゼロになっていた**。実測で往復20.2bps
（設定40bpsの約半分）。高値・安値は**約定値であって気配ではない**ので、
成行が約定する最良気配はそれより上でありうる。修正済み。

**この修正でコストは約2倍に戻り、既存の数字はすべて動く**（gross は動かない）。

### 選定はコストを見る（ATR% ではなく ATR円 で並べる）

実測（2026-08-24）で、選ばれた監視50銘柄の往復コストは
**0.052〜0.121 ATR で2.3倍ばらついていた**:

```
8012  1,156円  ATR% 3.30%  ATR 38.1円 → 0.0524 ATR
547A    447円  ATR% 3.71%  ATR 16.6円 → 0.1206 ATR   ← ATR%はほぼ同じ
```

スコアのボラティリティ項を ATR% → **ATR円** に差し替えた。
**重みの数も 0.40 という値も変えていない**（指標の差し替えのみ）ので
新しい自由度は増えない。旧スコアは `--legacy-score` で再現できる。

### 安全装置16項目の実装状況

実装済み（14項目）: #1 レバレッジ / #2 全建玉クローズ / #3 ショートのストップ必須 /
#4 日次 / #5 移動窓 / #6 累積DD / #7 ポジション制限 / #9 発注冪等性 /
#10 起動時 reconcile / #11 キルスイッチ / #12 売建可否 / #13 価格サニティ /
#14 銘柄フィルタ / #15 監査ログ

**未実装（2項目）**: #8 レートリミッタ / #16 ハートビート
— どちらも `broker/kabus.py` と `engine/live.py`（Stage B）に属する。

**全発注は `execution/order.py` の `submit()` を通る。** バイパス経路を作らない。
発注前に `execution/journal.py` へ注文IDを永続化することで、
ネットワーク断のリトライでも二重発注しない（#9）。

### 口座開設待ち（Stage A では書けない）

`broker/kabus.py` / `broker/paper.py` / `data/shortable.py` /
`data/recorder.py` / `engine/live.py` は kabuステーションAPI の実物が要る。

**ペーパートレード（Phase 5）にも口座は必要。** 利用料はすべて無料だが、
**入金は要る** — 信用取引口座に委託保証金30万円、API（Professionalプラン）に
1回以上の約定（docs/01-broker-api.md §1.1）。

ペーパー中は実発注しないので約定が積み上がらず、**API を失う恐れがある**。
月1回だけ最小単位で実売買して維持する。

5分足を貯める8週間のうちに開設しておくと Stage B の着手が遅れない。

### 次にやること

**レジーム診断（意思決定ログ47〜50）は打ち止め。次はエントリー根拠の再設計。**

竹（ORB+VWAP混合）は `--stress-test` で棄却が確定し、ユーザー提案
（相場局面に応じた手法選択）への縮小版検証も一区切りついた:

```
持続性: 前日wild→当日wild 60.0%（n=20）/ 前日calm→当日wild 44.4%（n=18）
```

VWAP乖離の成績は市場全体の calm/wild で偏る（calm 73.5% vs wild 99.0%）
ことは確認できたが、それを前日情報から予測できるかの持続性診断は
統計的に有意でなかった（z≈0.96・p≈0.34）。**否定ではなくデータ不足
による判定不能**——n=38ペアでは検出力が足りない可能性が高い
（`--stress-test` が99%の解像度に100シードを要求したのと同じ理由）。
80営業日に近づけば同じ診断（`--by-regime` 末尾の「持続性」）を
より大きな母数で再実行する余地を残す。市場全体レジームに基づく
先読みフィルタの設計はここでは進めない。

**VWAP乖離の2方向拡張（意思決定ログ51）は実データで両方棄却された
（意思決定ログ52）。**

```
変種A: VWAP乖離のみ2.0%              87件  65%  棄却
変種B: VWAP乖離1.5%+出来高確認1.5倍   65件  50%  棄却（ランダムと同水準）
```

「乖離を厳しくするほど良くなる」という単調な傾向（0.7%→75% / 1.0%→80% /
1.5%→95%）は2.0%で反転し、出来高確認も単独1.5%より悪化した。2つの独立した
予測がどちらも外れたことで、1.5%の95%自体が偶然だった可能性が強まった。
**VWAP乖離という手がかりはこの2方向で使い切った。** ORB（順張り）・VWAP乖離
（逆張り）とも、手元にあった発想からの改善余地は出尽くしたとみなす。

**次はギャップ・フェード仮説を安価に診断する（意思決定ログ53）。**
板情報は使えないので、残る手がかりのうち**まだ検証していないギャップ**
（前日終値と当日始値の差）を、**5分足を使わず日足だけ**（J-Quants無料で
最大2年、5分足の39〜80営業日よりずっと大きい母数）で予備検証する。

```
python scripts\measure_gap_fade.py
```

`|ギャップ|` の下限バケット（0.5%/1.0%/1.5%/2.0%）ごとに `fade_score`
（ギャップ方向と逆に動けば正＝フェード、伸びれば負＝ギャップ&ゴー）の
平均を出す。**まだ合否判定はしない。**

- **閾値を上げるほどベースラインよりはっきりフェード側に振れる** →
  桁の大きい母数で確認できたということなので、実際のデイトレ戦略
  （既存の `TakeIntraday` の枠組みを再利用——始値付近でのエントリー・
  当日クローズ・ATRベースの損切り）として実装する価値がある
- **バケット間で一貫しない、またはベースラインと大差ない** →
  この銘柄群・この市場ではギャップは手がかりにならない。
  出来高プロファイルなど他の発想に移る

#### 初回実行は判定に足るだけの母数が貯まっていなかった（実測で発覚・修正済み）

初回実行（全10,308ペア）は単調にフェード側へ振れる、これまでで
一番一貫した結果だった（ベースライン+0.0040%に対し0.5%→+0.0403% /
1.0%→+0.0702% / 1.5%→+0.0708% / 2.0%→+0.2237%）。

**ただし件数を銘柄数で割ると1銘柄あたり約78営業日しかなかった**
（10,308÷133銘柄）。この診断の存在理由だった「J-Quants無料2年分の
桁違いの母数」を活かせておらず、5分足の検証窓（39〜80営業日）に対して
2倍程度の優位しかなかった（意思決定ログ54）。

原因は `scripts/fetch_bars.py` の `DAILY_LOOKBACK_DAYS=120`。
Layer2の指標が要る20営業日ぶんを5分足の先頭より前から確保する、
という本来の目的だけに合わせた値で、`measure_gap_fade.py` がこの
日足キャッシュをそのまま再利用したため浅さを引き継いでいた。
yfinance の日足取得は期間による追加コストがほぼないため、**730日
（約2年）に拡張した。** `BarStore.write` は冪等なので再実行しても壊れない。

#### 母数6倍でも傾向は保たれた。ただし効果はコストと同水準（2026-08-26）

日足を2年に拡張して再測定（63,900ペア＝1銘柄あたり約480営業日）:

```
ベースライン  +0.0189%
0.5%  33,989  +0.0498%
1.0%  17,909  +0.0842%
1.5%   9,757  +0.0818%
2.0%   5,531  +0.1572%
```

**母数が6倍になっても、閾値を上げるほどフェード側へ振れる傾向は
保たれた。** VWAP乖離が2.0%で反転したのとは対照的で、これまでで
最も一貫している。

**ただし最大の2.0%バケットでも +15.7bps で、往復コスト（中央値29.7bps・
最安17.9bps）と同水準かそれ以下。**「統計的に本物の傾向」と「コスト後に
儲かる」は別問題——竹の実測でも損益分岐の勝率を割り込んで構造的に
負けていた（意思決定ログ36）のと同じ構造。

そこで `bucket_stats` を追加し、**バケットごとに gross / コスト / net を
bps で並べ、標準誤差と t値も出す**ようにした（意思決定ログ55）。
コストモデルは `autotrader.tick.spread_yen` をそのまま使い、診断ごとに
作り直さない。

#### 実測: 全バケットで net 負。ギャップ・フェードも棄却（2026-08-26）

```
|ギャップ|下限   件数     gross    コスト      net    t値
      0.5%   33,989   +4.98b   29.61b  -24.63b   4.6
      1.0%   17,909   +8.42b   29.95b  -21.53b   4.9
      1.5%    9,757   +8.18b   30.10b  -21.91b   3.2
      2.0%    5,531  +15.72b   30.11b  -14.39b   4.3
```

**t値3.2〜4.9で、フェード傾向そのものは統計的に本物。** だが
**全バケットで net が負**（最良の2.0%でも -14.4bps）。事前登録した
基準どおり棄却（意思決定ログ56）。

### コスト構造こそが問題だと分かった（意思決定ログ56）

3つの実験がすべて同じ形で終わった:

| 実験 | gross | コスト | net |
|---|---:|---:|---|
| 竹（ORB+VWAP混合） | ≈ **-0.3**bps | 44bps | 負 |
| VWAP乖離単独 1.5% | ≈ **+10**bps | 44bps | 負 |
| ギャップ・フェード 2.0% | **+15.7**bps | 30bps | 負 |

**見つかる優位が 0〜16bps、コストが 30〜44bps。** この比率が
どの実験でも一定であり、**個々のシグナルの良し悪しではなく
コスト構造の問題**を示している。シグナルを探し続けるより、
**コストの前提そのものを検証する**のが順序として正しい。

### 次にやること: スプレッドの当て推量を検証する

`tick.DEFAULT_SPREAD_TICKS = 2.0` は**プロジェクト最大の当て推量**で、
**この1つの数値が全コスト計算を線形にスケールする**（往復コスト・
`min_atr_yen`・Layer 2 のコストスコア・約定価格・損益分岐の勝率）。
「優位 vs コスト」の比率の**分母が正しいか**を確かめないまま
手法探しを続けるのは順序が逆。

日足の高安だけから実効スプレッドを推定する Corwin-Schultz（2012）を
`src/autotrader/spread.py` に実装した。**日足を2年に拡張済みなので
追加データは要らない。**

```powershell
git pull
python scripts\measure_spread.py
```

**見るのは「呼値の何本ぶんか」の中央値。**

- **2本前後** → 現行の想定は妥当。これまでの net 負はコストの
  過大評価ではなく本物。手法探しではなく**コスト構造**
  （保有期間を延ばす・株価帯を上げる）を見直す段階
- **1本前後** → コストを倍に見積もっていたことになる。
  **既存の全実験を再評価する必要がある**（gross は変わらないが net は変わる）
- **3本以上** → さらに厳しい。デイトレの往復では勝ち目が薄い傍証

**推定値は下限として扱う。** Corwin-Schultz は取引がまばらな銘柄ほど
下振れする。「思ったより狭かった」が出ても**楽観側には倒さない**
（規約「検証できないものは保守的な側に倒す」）。

#### 初回実行は133銘柄すべてが推定不能だった（原因特定・修正済み）

初回は「推定できた銘柄 0 / 推定不能（負の値）132」。**スプレッドが
ゼロなのではなく、推定量が機能していなかった。**

原因は**夜間ギャップ**。γ（2日を通した高安）には夜間に飛んだぶんが
そのまま入るが、β（各日の日中レンジの和）には入らない。γ ≫ β になると
推定値が負に振り切れる。原著の補正は「前日と当日の高安が**完全に
離れている**ときだけ平行移動」なので、日本株のように**日中レンジと
部分的に重なったまま飛ぶ**ケースでは発動しなかった（意思決定ログ58）。

合成データで切り分けた実測（真20bps・日中ボラ2%）:

```
夜間ボラ    原著の補正    終値→始値で補正
  0.0%        18.6bps        18.6bps
  0.5%         2.9bps        18.6bps
  1.0%       -34.5bps        18.6bps
  2.0%      -120.0bps        18.6bps
```

**原著が高値・安値しか使わないのは「それ以外が無い前提での一般性」の
ためであって、あるなら使ってよい。** `前日終値 ÷ 当日始値` の比で
当日の高安を割り戻し、夜間の飛びを正確に除く形に直した。
夜間ボラ0〜3%のすべてで18.6bpsと安定する。

#### 2度目の実行も「正だけ平均」で上振れしていた（訂正済み）

夜間ギャップを直した後の実測は「推定できた65 / 推定不能（負）67 /
正のみの中央値 6.6bps＝0.59tick」。**だが半分が負ということは、
正の65銘柄は「ノイズで上振れした側だけが生き残った」標本**であり、
その中央値は真の値を表さない。

合成データで確認したところ、**真のスプレッドが0bpsでも
「正のみの中央値」は +4.8bps を返した**（負87%）。真5bps→+6.1bps、
真10bps→+5.6bps と、真の値をほとんど反映していない。
**ペア単位で避けたはずの切り捨てバイアスを、銘柄単位で踏んでいた**
（意思決定ログ59）。

**全銘柄の β・γ をまとめて平均して最後に1回だけ解けば切り捨てが
起きない。** `corwin_schultz_pooled` を追加してこちらを主役にした。
銘柄ごとの推定は「負の割合」（推定がノイズ床にどれだけ近いかの目安）
としてのみ出す。

**観測された「負51%」を合成データで較正すると真は12〜13bps相当**
（負61%＝真10bps、負43%＝真15bps の間）で、呼値換算で**約1.1本**。
想定の2.0本より狭いが、「正のみ」が示した0.59本ほど狭くはない。

#### 3度目で確定: 推定不能。だが**結論は変わらない**（2026-08-26）

切り捨てを避けた `corwin_schultz_pooled` でも **-1.3bps（-0.13本）** と負。
較正値の12〜13bpsとも整合しない。**この銘柄群・この推定量では、
真のスプレッドはノイズ床に埋もれている**と結論した（意思決定ログ60）。
Stage A で測る手段は板を録る（楽天 RSS 等）以外にない。

**だが「測れない」で止まらず、決定に効くかを確かめた。**
gross はコストモデルに依存しないので、想定を変えた net は確実に出せる:

```
                    gross    2.0本    1.5本    1.0本    0.5本
竹（ORB+VWAP）     -0.3b   -44.3b   -33.3b   -22.3b   -11.3b
VWAP乖離 1.5%     +10.0b   -34.0b   -23.0b   -12.0b    -1.0b
ギャップ 2.0%     +15.7b   -14.4b    -6.9b    +0.7b    +8.2b
```

**竹と VWAP乖離はどの想定でも一度も正にならない。** ギャップ2.0%だけが
1.0本で符号が変わるが、較正値の約1.1本では実質ゼロで、取引可能な
優位ではない。**スプレッドをこれ以上精密に測っても、どの手法も救われない。**

`DEFAULT_SPREAD_TICKS = 2.0` は据え置く（規約「検証できないものは
保守的な側に倒す」）。感度表は `measure_gap_fade.py` の出力に常設した。

**結論: 問題はコストの見積り精度ではない。** この銘柄群・この時間軸で
得られる優位（0〜16bps）が、どんなコスト想定に対しても小さすぎる。

### TOPIX100 の細かい呼値が効いた（プロジェクト初の net 正）

**スプレッドが何本かは動かせなくても、呼値そのものは動かせる。**
東証の呼値は TOPIX100（Core30 + Large70）だけ細かく（0.1〜0.5円）、
`tick.py` にテーブルが実装済みなのに**一度も使っていなかった**。

まず「その銘柄群を50万円で扱えるか」を測った（`measure_topix100.py`）:

```
TOPIX100 99銘柄 → 通過7銘柄   （事前登録の基準は20銘柄 → 棄却）
  内訳: 株価上限1,250円超で92銘柄が脱落 / 流動性不足0 / 日足なし0
```

**棄却の理由は市場ではなく資金だった。** 株価上限1,250円は
`資金50万円 × 25%（安全装置#7）÷ 100株` の逆算値で、
**安全装置を緩めても解けない**（25%は日次ブレーカーからの逆算・意思決定ログ21）。

```
資金 → 使える TOPIX100 銘柄数
 50万円 →  7 (7%)  /  80万円 → 18 (18%)  / 120万円 → 35 (35%)
200万円 → 62 (63%) / 300万円 → 85 (86%)  / 500万円 → 88 (89%)
```

**だが増資を判断する前に「大型株でも優位が残るか」を先に測った。**
資金を1円も動かさずに測れるので、順序としてこちらが先
（`measure_gap_fade.py --topix100`・意思決定ログ65）。

### 実測: TOPIX100 でギャップ・フェードが初めて net 正になった

構成銘柄は**検証期間の開始時点（2024-08-26）**のものを使う。
現在の一覧で過去を測るのは `docs/03-universe.md` §4.2 が禁じている
サバイバーシップバイアス（**一度作り込んで自分で見つけた**・意思決定ログ66）。

```
  全日ベースライン: 44,712件 / gross +5.39bps

  |ギャップ|下限     件数     gross    コスト      net    t値
        0.5%     27,049   +10.30b    4.86b   +5.45b   9.7
        1.0%     16,051   +15.42b    4.78b  +10.64b  10.1
        1.5%      9,606   +20.74b    4.74b  +16.00b   9.6
        2.0%      5,786   +24.21b    4.75b  +19.46b   8.0
```

**プロジェクトで初めての net 正。** しかも予想と逆に **gross も大きい**
（一般ユニバースの2.0%は+15.72bps、TOPIX100 は+24.21bps）。
「大型株は ATR% が小さいので優位も縮む」という懸念は外れた。

サバイバーシップを外しても数字はほとんど動かなかった
（2.0%の net が +20.16 → **+19.46bps**）。**この優位は勝ち組効果ではない。**

### 呼値が本質。「株価が高い」だけでは足りない

同じ銘柄・同じ日を**通常銘柄の呼値（1円）**で評価し直した内訳:

```
  |ギャップ|下限     gross   TOPIX100呼値   通常呼値   net(通常)
        0.5%    +10.30b      4.86b     21.81b    -11.51b
        1.0%    +15.42b      4.78b     21.37b     -5.95b
        1.5%    +20.74b      4.74b     21.21b     -0.47b
        2.0%    +24.21b      4.75b     21.09b     +3.12b
```

**事前に立てた読み（「右端が正なら本質は資金の問題」）は外れた。**
通常呼値では2.0%バケットが辛うじて+3.12bps になるだけで、
取引可能な優位ではない。**TOPIX100 であることは付随条件ではなく必要条件。**

### 要件は2つ同時にかかる

| | 内容 | 動かせるか |
|---|---|---|
| (a) | TOPIX100 構成銘柄（呼値 0.1〜0.5円） | **動かせない**（市場が決める） |
| (b) | 株価1,250円超を建てられる資金 | **人間が判断すること** |

(a) が必要条件と確定したので、**残る変数は資金額だけ**になった。

### 実測: 上限見積りでも月利は目標に届かなかった

bps は「1回の取り分」しか言わない。月利は

```
月利 ＝ 1回の取り分 × 1日に建てられる回数 × 資金のうち建玉になる割合
```

で決まり、後ろ2つは資金額に強く依存する。`capacity_stats` で
**安全装置をバイパスせずに**（1銘柄25%・同時5・レバ1倍・単元株）測った:

```
      資金    使えた銘柄   枠/日    建玉率   月利(net)
  500,000円     19銘柄    0.87    17.1%    +1.28%
  800,000円     37銘柄    1.73    34.6%    +2.57%
1,200,000円     60銘柄    2.49    47.7%    +3.69%
2,000,000円     75銘柄    3.21    62.1%    +3.91%   ← 最大
3,000,000円     83銘柄    3.42    66.6%    +2.81%   ← 減る
```

**損切り・ブレーカー三層・5分足の約定モデル・売建の在庫確認を
どれも含まない上限見積りで最大 +3.91%。** 事前に登録したとおり
「ここで目標(+5%)に届かないなら実装すれば必ず下回る」に該当する。

**300万円で減っているのは注目に値する。** 建玉率は上がっている
（62.1→66.6%）のに月利が落ちる = 資金を増やして買えるようになった
銘柄のほうが成績が悪い。1回あたりの取り分を逆算すると:

```
 50万 37.4bps / 80万 37.1 / 120万 38.7 / 200万 31.5 / 300万 21.1
```

**120万円あたりまでは安定し、その先で急落する。** ただし
**この山を「最適資金」として採用しない**——手元データへの当てはめであり、
95.5%を棄却した規律（意思決定ログ46）と同じ理由で曲線の最大値を
後から選ぶことはしない。

### 方向別に決定的な非対称が出た

```
|ギャップ|下限    買建 件数    net       売建 件数    net
      0.5%     11,667   +6.43b      15,382   +4.70b
      1.0%      6,872  +15.75b       9,179   +6.80b
      1.5%      4,268  +26.63b       5,338   +7.51b
      2.0%      2,705  +35.95b       3,081   +4.99b
```

**買建は閾値とともに単調に伸び、売建はほぼ横ばいで非単調。**
買建（ギャップダウンのフェード）は安全装置#3（ショートのストップ必須）も
売建可能銘柄リスト（Stage A では取得不能）も要らない、実装が楽な側でもある。

### 実測: 市場ドリフトを控除すると優位の86%が消えた。棄却

```
全ペアの平均 始値→終値: -1.15bps

|ギャップ|下限   買建(素)   買建(控除)   売建(素)   売建(控除)
      0.5%     +6.43b     -0.93b     +4.70b     -1.48b
      1.0%    +15.75b     +1.92b     +6.80b     -0.56b
      1.5%    +26.63b     +4.93b     +7.51b     -1.21b
      2.0%    +35.95b     +4.94b     +4.99b     -4.12b
```

**買建は +35.95 → +4.94bps。** 買建と売建の差も 30.96 → 9.06bps に縮んだ。
事前登録した②「控除で買建が売建の水準まで落ちる」に該当する。

**ただし当初の読み（上昇局面だった）は外れている。**
全ペアの平均 `始値→終値` は **-1.15bps とわずかに負**で、
無条件の上昇ドリフトは存在しない。消えたのは**条件付きの**市場の動きで、
逆算すると **ある銘柄が2%以上ギャップダウンした日、市場全体がその日
およそ31bps 上昇して引けている**（海外の急落を受けて寄り安 → 日中に戻す、
という日本市場でよくある形）。

**実在する現象だが、性質がまったく違う。** 銘柄固有の平均回帰ではなく
**市場全体の値動きへの方向性の賭け**であり、レバレッジ1倍・安全装置16項目で
市場中立に近い設計を積んできた本システムとは別の商品になる。

控除後の +4.94bps に引き直すと月利は 0.4%程度。当時は控除前の
最大 +3.91% も目標（月利5%）に届いていなかったので棄却とした。

> **目標が年利25〜35%に変わり、この棄却理由の片方が覆った**（下記）。
> 控除の結果そのものは変わらないが、**それだけで棄却を維持するには
> 検証が足りていない**ので、追加の検定にかけている。

### t値は件数ベースだと過大だった（記録を訂正した）

意思決定ログ66・67に t値8.0〜10.1 を「統計的に本物」の根拠として
記録したが、**5,786件を独立な観測として計算したもの**だった。
上記のとおり**同じ日の銘柄は市場要因で強く相関している**ので、
独立な観測は件数ではなく**日数ぶんしかない**。

`clustered_stats` を追加し、日ごとに平均してから日をまたいで t値を出す。
出力の主表に「日数」「t値(日)」を並べた。**見るべきは t値(日) のほう。**

### 【人間の決定】目標を年利25〜35%に変更した（2026-08-27）

```
年利25% = 月利1.88% = 1日0.093%（50万円で日次465円）
年利35% = 月利2.53% = 1日0.125%（        日次626円）
```

旧目標の月利5%は**年利79.6%**、ストレッチの月利10%は**年利213.8%**だった。
月利ベースで小さく見える数字が、複利で年に効くと非現実的になっていた。

**副作用: 当日決済漏れの重みが増した。** 強制決済1回2,200円は、
旧目標では日次1,220円の約2日分だったが、**新目標では約5日分**。
`execution/close_all.py` の重要度は下がるどころか上がっている。

**合格の門は依然としてシャープレシオ**（`docs/07` §2）。目標リターンは
判断材料であって門ではない、という設計は変えていない。

### 目標変更で覆るのは棄却理由の一部だけ

**基準が動いた後に棄却済みの仮説を見直すのは、過学習を生む典型的な手順。**
何が覆り何が覆らないかを先に切り分けた（意思決定ログ74）。

| 実験 | 棄却の根拠 | 目標変更で |
|---|---|---|
| 竹（ORB+VWAP） | gross が負（コスト前で負け） | **覆らない** |
| VWAP乖離 1.5% | ランダム検定 95.5% < 必要99% | **覆らない**（信号の質の基準） |
| ギャップ（一般） | 全バケット net 負 | **覆らない** |
| ギャップ（TOPIX100） | ①月利+3.91% < +5% ②控除で86%消失 | **①だけ覆る** |

**①は完全に覆る。** +3.91%/月 = **年利58.6%**。80万円の +2.57%/月 = 年利35.6%、
買建のみでも120万円で年利29.2%・200万円で36.7% と新目標の帯に入る。

**②は目標と無関係なので残る。** ただし②だけで棄却を維持するには検証が足りない。
ギャップ・フェードには**まだかけていない検定が3つ**ある。

### 3検定の実測: 棄却でも合格でもなく、測り方に欠陥があった

#### ① 日クラスタ t値 — 土台が抜けた

```
       t値(件)   日数   t値(日)
0.5%     9.7     486     3.0
1.0%    10.1     486     3.3
1.5%     9.6     478     2.4
2.0%     8.0     456     0.6   ← 資金スイープはここで計算していた
```

**順序が逆転している。** net bps が最大の 2.0% が最も弱く、
最も強いのは 1.0%。件数ベースで8〜10あった t値は日クラスタで 0.6〜3.3 に落ち、
「t値(件) は過大」（意思決定ログ72）が実測で裏づけられた。

**年利+58.5%・Sharpe 2.77 は、すべて t=0.6 のバケットの上に乗っている。**
この 2.0% は「net bps が最大だったから」という**結果を見てから選んだ基準**で、
資金曲線の最大値を最適資金として採用しなかったのと同じ理由で採用できない。

#### ② 期間分割 — 文面上は通過、中身は減衰

```
          前半 net   t(日)    後半 net   t(日)
  0.5%     +8.90b     3.0      +2.32b    1.4
  1.0%    +16.65b     3.5      +5.66b    1.2
  1.5%    +23.97b     3.0      +9.83b    0.3
  2.0%    +28.89b     1.7     +12.20b   -0.6
```

事前登録した基準（両期間とも net 正 かつ 単調性）は**文面どおり通過**。
だが**後半は前半の42%まで減衰し、t値はどれも有意でない**。

2.0%の後半は net +12.20bps なのに t(日) が **-0.6**。
**典型的な日は負けており、該当銘柄が多い少数の日が平均を持ち上げている。**

#### ③ リスク指標 — 懸念は外れた。ただし高資金でDDが破れる

```
1,200,000円  Sharpe 2.94  BRK  4/486  最大DD  -6.8%  年利 +54.4%
2,000,000円  Sharpe 2.77  BRK  7/486  最大DD -13.8%  年利 +58.5%
3,000,000円  Sharpe 1.81  BRK 19/486  最大DD -19.6%  年利 +39.4%
```

**「5銘柄が同時に負けて運用不能になる」という懸念は外れた。**
ブレーカー発動は120万円で486日中4日にとどまる。

ただし**300万円では最大DD -19.6% が安全装置#6（累積DD -15%）を突破**する。
資金を増やすほどリスクが増える構造で、120万円（-6.8%）が上限。

### 実測: 閾値には耐えた。資金には耐えなかった

```
【後半 243営業日】年利 / シャープ
        資金        0.5%        1.0%        1.5%        2.0%
  500,000円  -12.3%/-1.2  -6.6%/-0.7  -2.8%/-0.4  +8.1%/ 1.6  ← 符号が変わる
  800,000円   +9.1%/ 0.6 +10.0%/ 0.6 +11.7%/ 0.9 +17.9%/ 1.7  ← 届かない
1,200,000円  +41.5%/ 2.1 +40.8%/ 2.1 +32.0%/ 1.8 +43.0%/ 2.6  ← 全閾値で届く
2,000,000円  +36.9%/ 1.7 +36.9%/ 1.7 +38.4%/ 1.8 +37.6%/ 1.9  ← 全閾値で届く
3,000,000円   +5.8%/ 0.3  +5.6%/ 0.3  +9.4%/ 0.4 +11.0%/ 0.5  ← 届かない
```

**事前登録した「閾値を変えても崩れない」は120万・200万では満たしている。**
200万は4閾値で 36.9〜38.4% と1.5ポイントしか動かず、このプロジェクトが
出した中で最も安定した結果。

**だが資金軸で壊れている。** 80万✗ / 120万○ / 200万○ / 300万✗ という
「真ん中だけ効く」形で、**全期間の山は200万、後半の山は120万**と
山の位置まで動く。ここで120万や200万を選ぶのは**資金曲線の最大値を
後から選ぶこと**で、意思決定ログ69で「採用しない」と登録済みの手順。

**最も決定的なのは、現在の資金50万円では後半に一度も届かないこと**
（-12.3%〜+8.1%、閾値で符号が反転）。

**数え方の問題もある。** 閾値4 × 資金5 × 期間3 × 方向2 で約120セルを見ており、
その中に目標超えがあること自体は、5変種に99%補正を要求した規律
（意思決定ログ45）からすると証拠にならない。

### 5分足を貯めている銘柄群が間違っていた

`fetch_bars.py` が5分足を取っていたのは `universe.json` の Layer 1
通過133銘柄（小型〜中型株）だけ。**この銘柄群では3手法とも棄却済み。**
唯一 net 正が出た TOPIX100 の5分足は**1本も貯まっていなかった**。

つまり「80営業日たまったら Phase 4」は、**棄却済みの銘柄群のデータを
貯め続ける計画**になっていた（意思決定ログ77）。

**yfinance の5分足は58日ぶんしか返さない。取らなかった週は永久に失われる。**
`load_topix100()` を追加し、収集対象に merge した。

### 次にやること

**資金判断はまだ行わない。** 上限見積り（損切り・ブレーカー三層・
5分足の約定モデル・売建の在庫確認をどれも含まない）でこの結果である以上、
次にやるべきは資金を増やすことではなく**実際の約定モデルで検証すること**。

```powershell
git pull
python scripts\measure_topix100.py --refresh   # 一覧が無ければ先に
python scripts\fetch_bars.py                   # 週1回。TOPIX100 も貯まる
```

**これまでの4実験はすべて日足、または5分足39営業日で行っている。**
十分な母数の日中データではまだ一度も検証していない。80営業日を超えれば、
日足では原理的に見えない時間帯構造（寄り付き30分・引け前30分など）を
扱えるようになる。

### 待っている間に、日足の前提そのものを検証する

**日足で測った実験はすべて「始値で建てて大引けで手仕舞う」に乗っている。**
だが実際の運用は2箇所でずれる:

| | 日足での仮定 | 実際の運用 |
|---|---|---|
| エントリー | 日足の始値 | 寄り付き（最初の5分足） |
| クローズ | 大引け（15:00） | **14:50**（安全装置#2） |

**14:50 は動かせない。** 閉じ損ねると強制決済で1注文2,200円
（目標リターンの約5日分）。つまり**大引け前10分の値動きは取りに行けない**。
引けの板寄せは平均回帰が完成しやすい場なので、優位がそこに集中していれば
**日足で測った数字はまるごと幻**になる。

```powershell
python scripts\measure_intraday_fill.py
```

**80営業日を待つ必要があるのは戦略の検証であって、仮定のズレの測定ではない。**
ズレは平均の差なので42営業日 × 226銘柄で十分に測れる。むしろ
**戦略を実装する前に測るべき**——ズレが大きいなら実装しても意味がない。

**判定基準は先に固定してある。**

- **14:50 と大引けで大きく違う** → 日足で測った優位の一部はそもそも
  取りに行けない。**前提を作り直す**
- **ほぼ同じ** → 14:50 の制約は無害。日足の実験結果はそのまま使える

出力の「1. 日足の始値 == 最初の5分足の始値 か」も見ること。
**ずれていれば、これまでの `gap_pct` の計算そのものが疑わしくなる。**

#### 実測: 始値が7割の日で一致しなかった（2026-08-27）

```
一致(0.5bps未満): 2,569件（27.7%）  ずれの中央値 26.77bps  最大 608.70bps
```

**これまで見つけた優位は0〜24bps。この誤差はそれより大きい。**
ギャップは「前日終値 vs 当日始値」で定義しており、その始値を日足から
取っている以上、**`gap_pct` の計算そのものが疑わしくなる**（意思決定ログ80）。

**原因は2つ考えられ、対処がまったく違った。**

| | 原因 | 意味 |
|---|---|---|
| ① | 最初の5分足が寄り付きのバーではない（9:00 が欠けている） | 5分足側の欠損。日足の始値は信用してよい |
| ② | 9:00 のバーはあるのに値がずれる | 価格の基準が違う。**日足ベースの実験すべてが疑わしい** |

#### 実測: ①だった。日足の実験は救われた（2026-08-27）

```
最初のバーが 09:00 かどうかで分ける:
  09:00 から   3,304件 / 一致 62.3% / ずれの中央値  0.00bps
  それ以降から 5,983件 / 一致  8.6% / ずれの中央値 39.42bps
```

**9:00 のバーがある日は、ずれの中央値が 0.00bps。完全に一致する。**
日足の始値は正しく、**これまでの `gap_pct` の計算も正しい**（意思決定ログ82）。

### だが5分足の寄り付きが64%の日で欠けている

```
最初のバーの時刻      最後のバーの時刻
  09:05  63.8%  ←欠損   15:20  96.7%
  09:00  35.6%           15:25   2.1%
```

**日足の実験には影響しないが、5分足での検証計画を直撃する。**
ギャップ・フェードは寄り付きで建てる戦略なので、9:00 のバーが無ければ
9:05 で建てるしかない。その時点で価格は**中央値39bps動いている**——
**追いかけている優位（0〜24bps）より大きい。**

東証は2024年11月から**15:30引け**なので、最後のバーが 15:20 なのも
引け際が欠けている疑いがある。

**8週間後ではなく今知る必要がある。** 貯めているデータが使えないなら、
貯め方を変えないといけない。

#### 実測: 日付でも銘柄でもなかった（2026-08-27）

```
1日あたりの本数: 中央値 65本（完全なら66本）
日付: 前半 37.6% / 後半 33.7%      → 差がない
銘柄: 0-25%:56 / 25-50%:125 / 50-75%:42 / 75-100%:3  → 二分されない
```

**セッション全体はほぼ揃っていて、欠けているのは寄り付きの1本だけ。**
事前に立てた2つの仮説はどちらも否定された。

**ラベルの向きも確定した。** もしラベルが区間終了なら「09:05」のバーは
9:00〜9:05 を表し、その始値は寄り付き値になって日足と一致するはず。
**一致しない（39bps ずれる）以上、ラベルは区間開始で、9:00〜9:05 の
バーは本当に存在しない**（意思決定ログ83）。

**データ源を変えない限り取れない。** なので問いが変わった——
「欠損をどう埋めるか」ではなく**「9:05 で建てても優位が残るか」**。

```powershell
python scripts\measure_intraday_fill.py
```

セクション3に**「最初のバー→14:50（実現可能）」**の行を足した。

#### 実測: 5分遅れるだけで19〜26bps 失う。棄却（2026-08-27）

```
【TOPIX100】
  日足の始値→大引け（従来の仮定）    +2.53bps  (t= 0.2)
  日足の始値→14:50               -0.19bps  (t=-0.0)
  最初のバー→14:50（実現可能）     -18.93bps  (t=-1.7)

【全銘柄】-25.99bps (t=-2.5)
```

**「実現可能」の行だけが、表の中で唯一 t値が有意に触れている。**
事前登録した基準（「大きく落ちるなら実務上の理由で死ぬ」）に該当する。

**死因は 14:50 ではなく寄り付き直後の数分だった。** 事前に疑っていた
「引け際に優位が集中している」は外れ（14:50 の影響は +2.48bps で無害）。

**ここに構造的な問題がある。** ギャップは
`(当日始値 - 前日終値) / 前日終値` なので、**始値が付いて初めて
シグナルが確定する**。つまり寄成注文（板寄せに参加する注文）は使えず、
最速でも「始値を見てから成行を出す」形になる。5分後には優位が消えている
以上、**秒単位の執行速度に成否がかかる戦略**であり、意思決定ログ13
（板を見る手法ほどレイテンシ勝負になり個人が不利）と同じ性質になる。

**ただし断定はしない。** 実運用なら 9:00:01〜9:00:05 で約定できる可能性が
あり、「5分遅れ」と「5秒遅れ」は別物。**だが Stage A に秒単位の分解能を
持つデータ源は無く、検証する手段が存在しない。**

棄却の理由は「優位が無い」ではなく、**「優位はあるが、この設計では
取りに行けず、取りに行けるかを確かめる手段も無い」**（意思決定ログ84）。

### 「数分の遅れに耐えるか」が新しい設計制約になった

ギャップ・フェードが死んだ理由は**ギャップ固有ではなく、優位が寄り付き
直後に集中していたこと**。同じ形の手法はすべて同じ理由で死ぬ。

`_report_decay` を追加した（意思決定ログ85）。エントリーを
**0/5/15/30/60分**遅らせたときの優位を並べる。**ギャップに依存しない**ので、
次に試す手法にもそのまま当てられる。

#### 実測: 減衰ではなく「崖」だった（訂正・2026-08-27）

```
銘柄群         0分      5分     15分     30分     60分
TOPIX100    -0.2b   -18.7b   -22.1b   -13.7b   -13.0b
```

**5分で崖のように落ちた後は横ばい。** 逆算すると 始値→5分 で約19bps
フェード方向に動き、5分→14:50 でほぼ同じだけ戻している。

**これは板寄せのオーバーシュートと整合する。** 板寄せは単一価格で約定
するので、その直後に実勢へ寄る。**ギャップ・フェードが日足で捉えていた
優位の正体は、その最初の数分の補正だった**（意思決定ログ86）。

**したがって「遅延そのもの」は問題ではない。** 5分でも60分でも成績は
変わらない。問題は**「板寄せの約定値を取れるか」の一点**。

### 正しい設計原則（意思決定ログ85を訂正）

| シグナルの確定時点 | 板寄せの価格 | 判定 |
|---|---|---|
| **前日引けまでの情報だけ** | 寄成注文で**取れる** | この制約から自由 |
| **当日の始値が要る** | 循環していて**取れない** | Stage A では成立しない |

「落ち方が速い手法は採らない」という前回の一般化は**不正確だったので
訂正する**。速さではなく、**板寄せに参加できるかどうか**が分かれ目。

**ギャップ・フェードは「永久に死んだ」のではない。** 寄り前気配を使えば
近似でき、それは **Stage B（kabuステーションAPI）では取れる**
（`docs/03-universe.md`）。正確には**「Stage A では検証も実行もできない」**。
ただし寄り前気配 ≠ 始値なのでシグナルは確実に劣化し、
どれだけ劣化するかは Stage B のデータが無いと測れない。

#### カットオフ修正後の 14:50 の影響

```
始値→14:50     +6.13bps（t=0.7）
始値→大引け     +8.61bps（t=0.9）
14:50→大引け    +2.48bps（t=0.8）  ← 修正前は+0.80bps
```

**取りに行けない部分は +2.48bps で、依然として有意ではない。**
14:50 の制約そのものは無害と見てよい。

#### カットオフの実装に誤りがあった（訂正済み）

**yfinance の5分足は区間の開始時刻でラベルされる。** `14:50` のバーは
14:50〜14:55 を表し、その終値は **14:55 の価格**。`timestamp <= 14:50` で
拾っていたので、**5分ぶん先読みしていた**（意思決定ログ81）。

区間が 14:50 までに終わるバー（開始 ≤ 14:45）だけを使うよう直した。
**ルックアヘッドは「注意して書く」では防げない**（規約7）という原則が、
バーのラベル解釈という思わぬところで効いた。

あわせてセクション3を TOPIX100 / Layer 1 に分けた。net 正が出たのは
TOPIX100 だけなので、混ぜると比較にならない。

### 実測: 前日反転も棄却。5家族すべてが同じ形だった

```
【TOPIX100】486営業日 / 48,107件
  |前日|下限   gross   コスト     net   t値(日)
      1.0%   +2.45b   4.86b  -2.41b    0.7
      2.0%   +4.97b   4.77b  +0.20b    1.1
      3.0%   +4.09b   4.71b  -0.62b   -0.1
      4.0%   +4.47b   4.66b  -0.19b   -0.7
```

事前登録した3条件すべて×（単調性なし・t値2未満・前半が全バケット負）。
Layer 1 は gross が -0.1〜-2.3bps で**コスト前から負**。

### 探しているのは14〜20bps、見つかるのは0〜5bps

| 実験 | gross | 判定 |
|---|---:|---|
| 竹（ORB+VWAP） | -0.3bps | コスト前で負 |
| VWAP乖離 1.5% | +10bps | 95.5% < 必要99% |
| ギャップ（一般） | +15.7bps | 全バケット net 負 |
| ギャップ（TOPIX100） | +24.2bps | 86%が市場β・執行不能 |
| 前日反転（TOPIX100） | +5.0bps | 単調性なし・有意でない |

**市場要因と執行制約を除くと、どれも 0〜5bps に落ち着く。**

毎回逆算せずに済むよう `diagnostics.required_gross_bps` を追加した。
**基準ではなく算術**——目標年利・コスト・建玉率から一意に決まる。

```
年利25%に要る gross     建玉率100%   建玉率62%（実測）
  TOPIX100（4.8bps）      14.1bps      19.8bps
  通常銘柄（21bps）       30.3bps      36.0bps
```

**3〜4倍の開きが5家族すべてで一貫している。** 個々のシグナルの
当たり外れではなく、**探索の設計そのものを問い直す段階**。

### 残っている未検証領域は3つ

| | 内容 | 誰が決めるか |
|---|---|---|
| ① | **十分な母数の日中構造**（5分足80営業日） | 収集中。あと約8週 |
| ② | **保有期間**（当日決済必須という前提そのもの） | **人間が判断すること** |
| ③ | **Stage B のデータ**（寄り前気配・板） | **人間が判断すること**（口座開設） |

**①は待つだけで進む。** これまでの5実験はすべて日足か5分足39〜42営業日で、
**日中の時間帯構造（寄り付き30分・引け前30分など）はまだ一度も検証していない**。
日足では原理的に見えない領域が丸ごと残っている。

**②は前提の変更になる。** デイトレ信用の手数料0・金利0は当日決済が条件で、
またぐと金利とオーバーナイトリスクが乗る。

> **前回「保有期間を延ばすとコストの重みが下がる」と書いたのは、
> 目標到達という観点では逆だったので訂正する**（意思決定ログ89）。
> N日持つと**回転が 1/N になる**ので、同じ日次リターンを出すには
> 1トレードあたり **N倍の net** が要る。一方コストは往復1回ぶんしか増えない。
>
> ```
> 年利25% / TOPIX100 / 建玉率100% / 金利年3%（未実測の仮定）
>    1日: コスト  4.8bps → 必要gross  14.1bps
>    3日: コスト  7.2bps → 必要gross  35.1bps
>    5日: コスト  9.6bps → 必要gross  56.1bps
>   10日: コスト 15.6bps → 必要gross 108.6bps
> ```
>
> **必要 gross は保有期間にほぼ比例して上がる。** gross が N倍より速く
> 伸びなければ目標には近づかない（多くのシグナルは √N 程度）。
> **「損を薄める」意味では正しいが、「目標に届く」意味では逆。**

```powershell
python scripts\measure_overnight_reversal.py --topix100
```

セクション4に**保有期間（1/2/3/5/10営業日）**の実測と、必要 gross との
○×が出る。金利は未実測なので感度表も併記（`DEFAULT_SPREAD_TICKS` と同じ扱い）。

#### 実測: gross は伸びたが、t値に重なりの欠陥があった（2026-08-27）

```
   保有     gross    コスト     net   t値(日)  必要gross  判定
   1日    +3.89b   4.77b  -0.88b    1.1    14.1b   ×
   3日   +20.85b   7.22b +13.63b    2.7    35.1b   ×
   5日   +31.52b   9.67b +21.85b    2.9    56.2b   ×
  10日   +27.24b  15.79b +11.45b    1.8   108.8b   ×
```

**gross は保有期間とともに強く伸びた**（5日で1日の8倍。√N の予想より速い）。
**だが t値の計算に欠陥があった**——3日保有を**毎日**エントリーして測ると、
D日に建てた玉と D+1・D+2 の玉は**同じ日を共有する**。日クラスタは
*エントリー日*でまとめているのでこの重なりを打ち消せず、
**独立な観測は日数ではなく日数/N ぶんしかない**。

| 保有 | 表示 t値 | 重なり補正後（÷√N） |
|---:|---:|---:|
| 3日 | 2.7 | **1.6** |
| 5日 | 2.9 | **1.3** |
| 10日 | 1.8 | **0.6** |

**補正すると有意なものは1つもない。** これは意思決定ログ72で直したのと
同じ構造の欠陥が、**横断方向ではなく時間方向**で起きたもの。

`diagnostics.non_overlapping_days` を追加した。あわせて**保有期間をまたいで
比較できる唯一の数字**として `net/日` と `年利換算` の列を足した——
net が保有期間で膨らむのは当然なので、そのまま比べると誤読する。

#### 実測: 3日以上は推定できない。2日は安定だが遠い（2026-08-27）

```
   保有    gross(全体)   位相の最小    位相の最大     振れ幅
   2日      +12.01b      +9.74b     +14.27b     +4.53b
   3日      +20.85b      +2.07b     +31.78b    +29.72b
   5日      +31.52b     +10.03b     +65.30b    +55.27b
  10日      +27.24b     -31.19b    +101.92b   +133.12b
```

**3日以上は振れ幅が gross そのものより大きい。** 10日では位相によって
**-31 〜 +102bps と符号まで反転**する。数え始める日を1日ずらすだけで
これだけ動くなら、**486営業日では3日以上の保有期間を推定できない**
（独立な窓が 486/N しかない）。

**安定しているのは2日だけ**（振れ幅は推定の38%）。だがその2日は
net/日 +3.01bps = **年利7.5%**、t=1.3、必要24.6bps に対し gross 12.01bps で×。

5日は「推定値31.5、ありうる範囲10〜65」で**必要な56.2bps をまたいでおり、
棄却でも合格でもなく判定不能**。

**保有期間の延長はここで打ち止め**——延ばせば推定できなくなり、
推定できる範囲では目標に届かない（意思決定ログ92）。

#### 修正が行き過ぎていた（訂正・意思決定ログ91）

補正版の実測は 5日 +31.52 → **+10.03bps**、10日 +27.24 → **+2.20bps** と激変した。
**だが効果が消えたのではない**——`non_overlapping_days` が先頭から N日おきという
**1つの位相しか取っておらず**、5日保有なら486日→97日で**データの8割を捨てた**うえ
「どの日から数え始めたか」に依存していた。

**2つの役割を混同していた。**

| | 正しいやり方 | 誤っていた実装 |
|---|---|---|
| **点推定**（gross/net） | 全観測を使う（不偏） | 1位相だけ ✗ |
| **t値** | 重なりを補正 | 1位相だけ ○ |

`phase` 引数を足し、**点推定は全観測・t値は全位相の平均**に直した。
あわせて `_report_phase_stability` を追加し、**位相を振ったときの gross の
最小・最大・振れ幅**を出す。

**振れ幅が gross そのものと同じ桁なら、その保有期間の推定は無意味。**
統計量を足すより直接的に効く。**再実行して振れ幅を見る。**

**これは「延ばせ」という提案ではない。** 1日以外は安全装置#2 を破り、
デイトレ信用の手数料0・金利0・貸株料0、翌日ギャップリスクの回避、
日次ブレーカーが前提にする「1日で損益が確定する」構造をすべて失う。
**材料を出すところまでが私の仕事。**

**③は口座開設。** ギャップ・フェードは寄り前気配があれば近似でき、
それは kabuステーションAPI で取れる（意思決定ログ86）。`CLAUDE.md` は
「5分足を貯める8週間のうちに開設しておくと Stage B の着手が遅れない」と
既に書いている。**利用料は無料だが入金は要る**（委託保証金30万円 + API に1約定）。

**私の推奨は ① を待ちつつ ③ を進めること。** ②は前提を大きく変えるので、
①の結果を見てからのほうが判断材料が多い。

### 【ユーザー提案】前提を変えて再探索する。まず地図を作る

「対象とする株価範囲・時価総額・取引量の条件を変えて新しい発見がないか」
という提案（意思決定ログ94）。**発想は正しい**——TOPIX100 の呼値という
最大の発見も、前提を疑ったことから出た。

**ただし手法 × ユニバース × 閾値を総当たりすると、多重比較で偶然を拾う。**
5変種の比較にすら99%補正が要り、資金×閾値×期間の約120セルを
「証拠にならない」と自ら記録している（意思決定ログ76）。

**なので最初にやるのは検定ではなく地図。**

```powershell
python scripts\measure_cost_landscape.py            # 地図だけ（データ不要）
python scripts\measure_cost_landscape.py --refresh  # 全上場銘柄の実測
```

往復コストは `呼値 × 2本 ÷ 株価 × 10,000` という**株価の決定関数**で、
データを1件も見ずに書ける。**地図は偽陽性を出せない。**

#### ゼロデータで分かったこと

```
通常銘柄（3,000円以下は呼値1円）
    500円 → 40.0bps      2,000円 → 10.0bps
  1,000円 → 20.0bps      2,999円 →  6.7bps  ← 最安
  1,250円 → 16.0bps      3,001円 → 33.3bps  ← 崖（呼値1→5円）
```

**呼値は絶対額なのでコストは株価ののこぎり波になる。**
そして**株価上限1,250円（資金50万円 × 25% ÷ 100株）は、通常銘柄の
最も高いコスト帯に我々を閉じ込めていた**——Layer 1 で測った
21〜30bps はそこから来ている。

```
   資金       株価上限   最安の株価    通常   TOPIX100
 500,000円   1,250円    1,250円   16.0b     2.0b
 800,000円   2,000円    2,000円   10.0b     2.0b
1,200,000円   3,000円    3,000円    6.7b     2.0b
```

**2,500〜3,000円の通常銘柄は 6.7〜8bps で、TOPIX100 の実測平均
4.75bps に匹敵する。** そこには資金120万円が要る。

#### 抜けていた領域

| 銘柄群 | 株価帯 | コスト | 検証 |
|---|---|---|---|
| Layer 1（小型〜中型） | ≤1,250円 | 21〜30bps | 3手法とも棄却 |
| TOPIX100（大型） | 高い | 4.8bps | 2手法とも棄却 |
| **中型・小型の高株価帯** | **2,000〜3,000円** | **6.7〜10bps** | **未検証** |

しかも中型・小型は大型より非効率が残りやすい。

#### 規律: ユニバースは成績で選ばない

**コスト・流動性・銘柄数という構造的な基準だけで選び、選んでから
1つだけ事前登録して検定する。** 成績で選ぶのは資金曲線の最大値を
後から選ぶのと同じ（意思決定ログ69）。

`MarketRow` に成績の欄を持たせないことで、経路を構造的に塞いである。

### 棄却されずに残っている資産

手法が3つ棄却されても、**手法に依存しないものは残る**。

- **TOPIX100 の呼値はコストを1桁下げる**（21.09 → 4.75bps）。
  これは市場の制度であって仮説ではないので、次の手法にもそのまま効く
- **株価上限1,250円が TOPIX100 の92/99銘柄を弾く**。
  資金を増やす以外に解けない（安全装置#7 の逆算値・意思決定ログ21）
- **`capacity_stats`** — bps を年利・シャープ・ブレーカー発動日に翻訳する。
  安全装置をバイパスしないので、どの手法にも同じ物差しを当てられる
- **診断の道具立て** — ランダム検定・多重比較補正・期間分割・
  サバイバーシップ回避・市場ドリフト控除・日クラスタ t値


**`scripts/fetch_bars.py` を週1回実行する。** yfinance は58日ぶんしか
返さないが `BarStore` がマージするので、3ヶ月で約100営業日になる。
**TOPIX100 も収集対象に入った**（意思決定ログ77）。実測 2026-08-27 時点で
227銘柄 / 42営業日。

**Phase 4 の開始判定は「80日以上ある銘柄が50件（監視枠）以上」**
（意思決定ログ78）。以前は全銘柄の和集合で数えており、1銘柄でも80日あれば
「揃った」と出る壊れ方をしていた。出力の「銘柄ごとの営業日数」を見ること。

工程は `docs/00-overview.md` を参照。
