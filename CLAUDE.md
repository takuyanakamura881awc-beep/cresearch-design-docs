# CLAUDE.md

日本株の自動売買ツール。資金50万円、三菱UFJ eスマート証券の kabuステーションAPI で発注する。

**このプロジェクトは誤りが金銭損失に直結する。** 一般的なソフトウェアと違い、
バグは「動かない」では済まず、意図しない発注や建玉の閉じ忘れがそのまま現金の損失になる。

## Stage A / Stage B

工程は2段階（`docs/09-data-sources.md`）。**今は Stage A。**

| | Stage A | Stage B |
|---|---|---|
| 口座・API | **不要** | 必要（運用資金は入れない） |
| データ源 | J-Quants 無料 + yfinance | 上記 + kabuステーションAPI |
| Phase | 1〜3 | 4〜6 |
| 判定 | 戦略に見込みがあるか | システムが信頼できるか |

**Stage A では以下が使えない。** 実装時に前提を混同しないこと:

- 板情報（bid/ask/厚み）→ 約定モデルは**保守的な側に倒す**（片道20bps）
- 寄り前気配 → 選定は**前日引け時点の情報のみ**（`universe/selector.py` の `StageAFeatures`）
- 一般信用デイトレの売建可能銘柄リスト → 流動性上位で代理
- 発注系全般 → Stage B で検証する

---

## 絶対に守ること

### 1. レバレッジ1倍の不変条件

```
建玉総額（買建 + 売建の合計） ≤ 現金残高
```

**全発注は `src/autotrader/risk/leverage.py` を必ず通る。バイパス経路を書かない。**
信用取引を使いながら使用金額を現物と同水準に抑えることが、本システムの安全性の土台。

### 2. 建玉は必ず当日中に閉じる

デイトレ信用の建玉を閉じ損ねると、**翌営業日に強制決済され1注文2,200円**。
月利目標の約2日分が消える。

`execution/close_all.py` が最重要コードパス。大引け（14:50）と緊急停止で共用する。
**クローズ後は `GET /positions` で残存を実測確認する**（成功したはずと仮定しない）。

### 3. ショート建玉はストップ注文なしで作らない

空売りは理論上損失無限大。レバレッジ1倍でもこの性質は変わらない。

### 4. 認証情報をコードに書かない

`.env`（`.gitignore` 済み）から環境変数経由でのみ読む。
`data/`（市場データ）と `KILL`（キルスイッチ）もコミットしない。

### 5. コストをゼロにしない

デイトレ信用は手数料0・金利0・貸株料0だが、
**スリッページとスプレッドは必ずモデル化する**。
コストゼロのシミュレーションを書くと本番で必ず乖離する。

**検証できないものは必ず保守的な側（成績が悪くなる側）に倒す。**
Stage A は板がないため、スリッページを Stage B より厚く見積もる（20bps vs 10bps）。
楽観的な約定モデルでバックテストに勝つのは簡単だが、それは目的ではない。

### 6. データ取得の規約（yfinance の落とし穴対策）

`data/yahoo.py` に実装済み。**新しくデータ取得を書くときも必ず守る**
（詳細は `docs/09-data-sources.md` §2.5）。

- **空の DataFrame を握り潰さない。** yfinance はブロックされても例外を投げず空を返す。
  「データなし」と「ブロック」が区別できない以上、**例外にして安全側に倒す**
- **`auto_adjust=True` を必須にする。** 未指定だと分割前後で価格が不連続になり、
  ATR% や売買代金が壊れて架空の急騰を誤検出する
- **欠損は `pd.notna()` で判定する。** `is None` では `NaN` を検出できない
- **tzキャッシュはプロセス固有ディレクトリに隔離する。** 複数プロセスが同時に触ると落ちる
- **取得可能期間の超過は黙って切り詰めずエラーにする。** 取れたつもりで欠損しているのが最悪
- **単一データソースに依存しない。** `data/base.py` の `FallbackDataSource` を使い、
  **フォールバックの発動は必ずログに残す**
- **列構造を銘柄数で仮定しない。** yfinance は単一ティッカーでも MultiIndex を返す。
  仮定すると全行が欠損扱いで捨てられ、**エラーなく空になる**（実際に踏んだ）
- **J-Quants は V2。** パス（`equities/master` / `equities/bars/daily`）も
  レスポンス構造（`{"data": [...]}`）も項目名（短縮形）も V1 から変わっている
- **レート制限はバーストせず均等間隔で送る。** スライディングウィンドウだと
  サーバ側の窓の境界で超過して 429 になる（実際に踏んだ）
- **レート制限エラーを「データなし」と混同しない。** 測定ループで混同すると
  **測定値そのものが嘘になる**（実測の遅延が84日→88日にずれた）。
  `RateLimitError` を分離してあるので、受けたら中断して「測定不能」と報告する

### 7. 未来のデータを参照しない

バックテストは**構造的に**ルックアヘッドを防ぐ。「注意して書く」では不十分。
寄り前の判断に使ってよいのは、前日大引けまでの確定情報と当日の寄り前気配だけ。

---

## ドキュメント

作業前に該当するものを読む。

| ファイル | 内容 |
|---|---|
| `docs/00-overview.md` | 全体像・工程・**意思決定ログ** |
| `docs/01-broker-api.md` | kabuステーションAPI の仕様と制約 |
| `docs/02-margin-rules.md` | 信用取引ルール・レバ1倍の不変条件・当日決済要件 |
| `docs/03-universe.md` | 銘柄選定の2層構造・プレミアム枠・バイアス回避 |
| `docs/04-strategies.md` | 竹（メイン）・松/梅（将来）の手法定義 |
| `docs/05-risk-management.md` | **安全装置16項目** |
| `docs/06-operations.md` | 運用手順・障害時対応・キルスイッチ |
| `docs/07-go-live-criteria.md` | **実弾移行の合格基準（後から緩めない）** |
| `docs/08-development-harness.md` | 開発ロールとモデル選択 |
| `docs/09-data-sources.md` | **データ源と Stage A/B の2段階構成** |

---

## 開発ハーネス

`.claude/agents/` にロールを定義してある（詳細は `docs/08-development-harness.md`）。

| ロール | モデル | 権限 | 責務 |
|---|---|---|---|
| `architect` | Opus 5 | 読み書き | 設計判断・インターフェース定義 |
| `implementer` | Sonnet 5 | 読み書き | 確定した仕様の実装 |
| `data-engineer` | Sonnet 5 | 読み書き | データ取得・整形・永続化 |
| `test-writer` | Sonnet 5 | 読み書き | テスト作成（仕様から書く） |
| `reviewer` | Opus 5 | 読み取りのみ | コードの正しさ検証 |
| `risk-auditor` | Opus 5 | 読み取りのみ | 安全装置16項目の監査 |
| `backtest-validator` | Opus 5 | 読み取りのみ | バイアス検出・過学習検証 |

**判断が要る仕事に Opus、手が要る仕事に Sonnet。**
検証系は read-only にして、指摘の独立性を保っている。

### 必ずゲートを通すタイミング

- Phase 3 完了判定 → `risk-auditor`
- ペーパートレード開始前 → `risk-auditor`
- バックテスト結果の採用判断 → `backtest-validator`
- **実弾移行前 → `risk-auditor` と `backtest-validator` の両方**

---

## 人間が判断すること（エージェントに委ねない）

- 安全装置の閾値を緩める
- 合格基準（`docs/07-go-live-criteria.md`）を変える
- 実弾に移行する / 投入額を増やす
- ブレーカー発動後の再開承認
- 対象市場・証券会社を変える

---

## コーディング規約

- Python 3.11+ / `ruff`（line-length 100）/ `mypy --strict`
- 型注釈を必ず付ける
- 外部API のレスポンスは `pydantic` で検証する（そのまま信用しない）
- エラーを握り潰さない（`except: pass` を書かない）
- 金額計算は浮動小数点の誤差累積に注意する
- **既存の関数を探してから新規に書く**（同じことをする関数を二つ作らない）

### 実行

```bash
pip install -e ".[dev]"
ruff check .
mypy
pytest
```

---

## 現在のステータス

**Phase 1〜3 完了。Phase 4 は5分足が80営業日ぶん貯まってから。**

- Phase 1: データ基盤（`data/`）。全項目を実機で実測済み
- Phase 2: Layer 1/2（`universe/`）、バックテスト基盤（`engine/` `broker/replay.py`）
- Phase 3: 竹（`strategy/take_intraday.py`）、ブレーカー三層（`risk/limits.py`）

### 実測で確定した値（2026-08-23 / 基準日 2026-05-29）

```
全上場 4,451 → プライム 1,565 → 貸借 1,483 → Layer 1 通過 133銘柄
  → ATR% 2〜5.33% で 106銘柄 → 日次の監視枠 50/50 が埋まる
```

| 値 | 確定値 | 由来 |
|---|---|---|
| 株価上限 | **1,250円** | 資金 × 25%(#7) ÷ 100株 |
| 通常枠の上限 | **1,000円** | 資金 ÷ max_concurrent(5) ÷ 100株 |
| 流動性下限 | **3億円** | 100銘柄を超える最も厳しい水準（実測） |
| ATR% の下限 | **2%** | 往復コスト（片道20bps×2）の5倍 |
| ATR% の上限 | **5.33%** | 日次ブレーカー(2%) ÷ (25% × 損切り1.5倍) |
| 移動窓の損失 | **直近15日で -5%** | 日次(-2%)の2.5倍・累積DD(-15%)の1/3 |

**上限比率25%は動かさない。** 日次ブレーカー(-2%)と1.5×ATR損切りからの
逆算値で、40%にすると ATR 4% の銘柄で1敗が当日を終わらせる。

### 竹の実データ検証（39営業日・パラメータ未検証）

| | ブレーカー有効 | 無効 |
|---|---:|---:|
| トレード数 | 152 | 507 |
| 総リターン | **-5.10%** | **-16.00%** |

**ブレーカーが損失を1/3に抑えた。** これが Phase 3 の主目的。
移動窓(#5)が11日目に -5.01% で発動し、**-15.57%（#6 まで到達）を -5.10% に抑えた**。
成績そのものは評価しない（パラメータ未検証、39営業日は年率の15%）。

**トレード数の目標値は持たない。** 実測1日12〜13回で日次ブレーカーは0日。
縛るのは日次損益(-2%)であって回数ではない。

### 安全装置16項目の実装状況

実装済み（14項目）: #1 レバレッジ / #2 全建玉クローズ / #3 ショートのストップ必須 /
#4 日次 / #5 移動窓 / #6 累積DD / #7 ポジション制限 / #9 発注冪等性 /
#10 起動時 reconcile / #11 キルスイッチ / #12 売建可否 / #13 価格サニティ /
#14 銘柄フィルタ / #15 監査ログ

**未実装（2項目）**: #8 レートリミッタ / #16 ハートビート
— どちらも `broker/kabus.py` と `engine/live.py`（Stage B）に属する。

**全発注は `execution/order.py` の `submit()` を通る。** バイパス経路を作らない。
発注前に `execution/journal.py` へ注文IDを永続化することで、
ネットワーク断のリトライでも二重発注しない（#9）。

### 次にやること

**`scripts/fetch_bars.py` を週1回実行して5分足を貯める。**
yfinance は58日ぶんしか返さないが `BarStore` がマージするので、
3ヶ月で約100営業日になる。80営業日を超えたら Phase 4
（ウォークフォワードでのパラメータ確定）に進む。

工程は `docs/00-overview.md` を参照。
